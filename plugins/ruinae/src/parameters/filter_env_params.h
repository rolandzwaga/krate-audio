#pragma once
// Filter envelope parameters. Implementation lives in the shared, config-driven
// env_params.h (FilterEnvParams + handle/register/format/save/load wrappers).
// Kept as a thin re-export so existing include sites resolve unchanged.
#include "parameters/env_params.h"
