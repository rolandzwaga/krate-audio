--- /tmp/controller.h	2026-03-18 19:01:06.221837307 +0000
+++ /tmp/innexus_patch/controller.h	2026-03-18 19:10:17.332455174 +0000
@@ -24,8 +24,10 @@
 #include "vstgui/plugin-bindings/vst3editor.h"
 #include "vstgui/lib/cvstguitimer.h"
 
+#include <atomic>
 #include <filesystem>
 #include <memory>
+#include <mutex>
 #include <string>
 
 namespace Steinberg {
@@ -181,8 +183,13 @@
     std::unique_ptr<Krate::Plugins::UpdateChecker> updateChecker_;
 
     // Display data pipeline (FR-048)
-    DisplayData cachedDisplayData_{};
+    // pendingDisplayData_ is written by the Data Exchange callback and read on the
+    // UI timer thread, so protect it when background dispatch is enabled.
+    DisplayData cachedDisplayData_{};      // UI-thread copy consumed by views
+    DisplayData pendingDisplayData_{};     // latest block received from host
     Steinberg::Vst::DataExchangeReceiverHandler dataExchangeReceiver_{this};
+    std::mutex pendingDisplayDataMutex_;
+    std::atomic<uint32_t> pendingDisplayFrameCounter_{0};
 
     // Display timer (FR-049)
     VSTGUI::SharedPointer<VSTGUI::CVSTGUITimer> displayTimer_;
@@ -239,7 +246,7 @@
 
     // Stale data detection: count consecutive timer ticks with no new frame
     int staleTickCount_ = 0;
-    static constexpr int kStaleTickThreshold = 3; // ~90ms at 30ms timer
+    static constexpr int kStaleTickThreshold = 15; // ~450ms at 30ms timer
 };
 
 } // namespace Innexus
--- /tmp/controller.cpp	2026-03-18 19:01:14.522414968 +0000
+++ /tmp/innexus_patch/controller.cpp	2026-03-18 19:10:17.335739925 +0000
@@ -1366,15 +1366,20 @@
     Steinberg::uint32 /*blockSize*/,
     Steinberg::TBool& dispatchOnBackgroundThread)
 {
-    // Dispatch on UI thread so we can safely update cachedDisplayData_
-    // without synchronization (timer also fires on UI thread).
-    dispatchOnBackgroundThread = false;
+    // Let the host deliver Data Exchange blocks off the UI thread. We only copy
+    // into a pending buffer here; the UI timer remains responsible for touching
+    // VSTGUI views. This avoids starving the queue when the host's message thread
+    // is busy.
+    dispatchOnBackgroundThread = true;
+    pendingDisplayFrameCounter_.store(0, std::memory_order_release);
+    lastProcessedFrameCounter_ = 0;
+    staleTickCount_ = 0;
 }
 
 void PLUGIN_API Controller::queueClosed(
     Steinberg::Vst::DataExchangeUserContextID /*userContextID*/)
 {
-    // Nothing to clean up
+    pendingDisplayFrameCounter_.store(0, std::memory_order_release);
 }
 
 void PLUGIN_API Controller::onDataExchangeBlocksReceived(
@@ -1383,15 +1388,29 @@
     Steinberg::Vst::DataExchangeBlock* blocks,
     Steinberg::TBool /*onBackgroundThread*/)
 {
-    // Copy the latest block's DisplayData into the cached buffer.
-    // If multiple blocks arrived, use the last one (most recent data).
+    DisplayData newest{};
+    bool gotBlock = false;
+
+    // Copy the most recent block into a pending buffer. The UI thread will pick
+    // it up on the next timer tick and update the custom views.
     for (Steinberg::uint32 i = 0; i < numBlocks; ++i)
     {
         if (blocks[i].data && blocks[i].size >= sizeof(DisplayData))
         {
-            std::memcpy(&cachedDisplayData_, blocks[i].data, sizeof(DisplayData));
+            std::memcpy(&newest, blocks[i].data, sizeof(DisplayData));
+            gotBlock = true;
         }
     }
+
+    if (!gotBlock)
+        return;
+
+    {
+        std::lock_guard<std::mutex> guard(pendingDisplayDataMutex_);
+        pendingDisplayData_ = newest;
+    }
+
+    pendingDisplayFrameCounter_.store(newest.frameCounter, std::memory_order_release);
 }
 
 // ==============================================================================
@@ -1681,8 +1700,11 @@
     // Update sample load panel visibility (cheap check every 30ms)
     updateSampleLoadVisibility();
 
+    const auto newestFrameCounter =
+        pendingDisplayFrameCounter_.load(std::memory_order_acquire);
+
     // Check if we have new data (frame counter changed)
-    if (cachedDisplayData_.frameCounter == lastProcessedFrameCounter_)
+    if (newestFrameCounter == 0 || newestFrameCounter == lastProcessedFrameCounter_)
     {
         // No new frame — track staleness. When the host stops calling
         // process() (e.g. playback stopped), clear the display so it
@@ -1712,6 +1734,11 @@
         return;
     }
 
+    {
+        std::lock_guard<std::mutex> guard(pendingDisplayDataMutex_);
+        cachedDisplayData_ = pendingDisplayData_;
+    }
+
     staleTickCount_ = 0;
     lastProcessedFrameCounter_ = cachedDisplayData_.frameCounter;
 
--- /tmp/processor.h	2026-03-18 19:01:59.948007468 +0000
+++ /tmp/innexus_patch/processor.h	2026-03-18 19:10:17.336446203 +0000
@@ -693,6 +693,8 @@
     std::unique_ptr<Steinberg::Vst::DataExchangeHandler> dataExchange_;
     DisplayData displayDataBuffer_{}; // Processor-side buffer, no atomic needed
     uint32_t displayFrameCounter_ = 0; // Monotonic counter across all sends
+    int displaySendIntervalSamples_ = 0;
+    int displaySendAccumulatorSamples_ = 0;
 
     // =========================================================================
     // Processing State
--- /tmp/processor_messages.cpp	2026-03-18 19:01:10.937879223 +0000
+++ /tmp/innexus_patch/processor_messages.cpp	2026-03-18 19:10:17.336903969 +0000
@@ -23,7 +23,7 @@
         auto configCallback = [] (Steinberg::Vst::DataExchangeHandler::Config& config,
                                   const Steinberg::Vst::ProcessSetup& /*setup*/) {
             config.blockSize = static_cast<Steinberg::uint32>(sizeof(DisplayData));
-            config.numBlocks = 2;
+            config.numBlocks = 8;
             config.alignment = 32;
             config.userContextID = 0;
             return true;
@@ -53,8 +53,24 @@
 // ==============================================================================
 // sendDisplayData() -- Display data pipeline (M7: FR-048)
 // ==============================================================================
-void Processor::sendDisplayData(Steinberg::Vst::ProcessData& /*data*/)
+void Processor::sendDisplayData(Steinberg::Vst::ProcessData& data)
 {
+    if (!dataExchange_ || data.numSamples <= 0)
+        return;
+
+    // The UI timer updates at 30ms, so sending one display block per process()
+    // call is overkill and can overflow a tiny queue in hosts that only drain
+    // the controller side intermittently. Throttle the transport to roughly the
+    // same cadence as the UI.
+    if (displaySendIntervalSamples_ <= 0)
+        displaySendIntervalSamples_ = std::max(1, static_cast<int>(sampleRate_ * 0.03));
+
+    displaySendAccumulatorSamples_ += static_cast<int>(data.numSamples);
+    if (displaySendAccumulatorSamples_ < displaySendIntervalSamples_)
+        return;
+
+    displaySendAccumulatorSamples_ %= displaySendIntervalSamples_;
+
     // Populate display buffer from current processor state
     const auto& frame = morphedFrame_;
     const int numPartials = std::min(frame.numPartials,
@@ -99,17 +115,17 @@
     displayDataBuffer_.frameCounter = ++displayFrameCounter_;
 
     // Send via DataExchangeHandler (uses native API if host supports it,
-    // falls back to IMessage transparently)
-    if (dataExchange_)
+    // falls back to IMessage transparently). If the queue is temporarily full,
+    // just drop this visual frame and keep the audio thread moving.
+    auto block = dataExchange_->getCurrentOrNewBlock();
+    if (block.blockID == Steinberg::Vst::InvalidDataExchangeBlockID
+        || block.data == nullptr)
     {
-        auto block = dataExchange_->getCurrentOrNewBlock();
-        if (block.blockID != Steinberg::Vst::InvalidDataExchangeBlockID
-            && block.data != nullptr)
-        {
-            std::memcpy(block.data, &displayDataBuffer_, sizeof(DisplayData));
-            dataExchange_->sendCurrentBlock();
-        }
+        return;
     }
+
+    std::memcpy(block.data, &displayDataBuffer_, sizeof(DisplayData));
+    dataExchange_->sendCurrentBlock();
 }
 
 // ==============================================================================
