# Vorago Phase 12 Compliance — vorago-phase12-parameters

**Overall status: COMPLETE** — every FR/SC/constraint item below carries a verified `pass` verdict; there are zero fail or partial items.

Spec: `specs/vorago-phase12-parameters/spec.md`. Plan: `specs/vorago-phase12-parameters/plan.md`. Tasks: `specs/vorago-phase12-parameters/tasks.md`. Evidence logs live under `specs/vorago-phase12-parameters/artifacts/`.

## Compliance table

| ID | Verdict | Evidence |
|---|---|---|
| FR-001 | pass | `dsp/include/krate/dsp/systems/vorago_macro_matrix.h:1006-1029`: `setTargetBase` no-ops when `i >= kNumTargets \|\| !isFiniteBits(base)` (`isFiniteBits` -> `detail::isFinite` at `:1147`, no `std::isnan`); `resetTargetBases` fills `hasOverride_` false (`:1016-1019`); `getTargetBase` returns `hasOverride_[i] ? baseOverride_[i] : literalBaseFor(target)` (`:1028`). `evaluateAll` seeds `value[i] = hasOverride_[i] ? baseOverride_[i] : row.base` (`:1183`). No rescaling. The 'NOT PROVIDED, DELIBERATELY' banner is replaced by the override contract (`:58-73`). `VoragoMacro_TargetBaseOverride` passed in the `dsp_systems_tests` run of the 10 Phase 12 cases (67563 assertions). |
| FR-002 | pass | `vorago_macro_matrix.h:1183` takes the `row.base` path when there is no override, so the no-override arithmetic is unchanged. The static_asserts remain at `:1215-1234`, including `everyRowSharesOneBasePerTarget` at `:1225`. `dsp/tests/unit/systems/vorago_param_surface_test.cpp:287` SECTION `NoOverrideIsBitIdentical` compares a reset matrix with an untouched one over 8 random vectors using exact `==`. Passed. |
| FR-003 | pass | `vorago_engine.h:160-189` declares `struct VoragoVoiceParams` with all 11 listed fields and `kFieldCount = 31` (`:188`). `static_assert is_trivially_copyable` at `:190`. No field names a `VoragoMacroTarget`. The initializers (0.45, 0.10, StoneChamber/SteelTank, FilteredWind/GranularDust/Direct/MetallicHiss, Hybrid) match `vorago_voice.h` `prepare`: `setSpectralGravity(0.10f)`, `setStereoSpread(0.45f)`, the four `setSourceModel` calls, `AnchorMode::Hybrid`, and the material calls at `:626-627`. `applyVoiceParams` at `:859-880` loops `v < kMaxVoices`. |
| FR-004 | pass | `vorago_voice.h:1159-1232` has 7 forwarders: `setCloudSpectralGravity`, `setNoiseSourceModel`, `setNoiseSourceType`, `setNoiseCombTuning`, `setNoiseCombFeedback`, `setResonanceAnchorMode`, `setEcologyLoopFilterMode`. The bare forwards rely on owner early-outs (`harmonic_cloud.h:478-485` `if (v == gravity_) return;`; `noise_organism.h:1720` is a no-op with no duck). The guarded forwards: comb tuning rejects non-finite and early-outs on the last request (`:1176-1190`); comb feedback uses a latch-aware early-out (`:1196-1209`); anchor mode (`:1214-1219`) and loop filter (`:1223-1231`) compare against the getter. `VoragoVoice_Phase12Forwarders` and `VoragoEngine_RepeatedBroadcastIsInert_Short` passed. |
| FR-005 | pass | `vorago_engine.h:914-926`: `setSubToneLevelDb` returns on `tone >= kNumTones \|\| !detail::isFinite(dB)` and on `dB == subToneBaseDb_[tone]`. It writes only tone `t` as base + `subToneOffsetDb_`. `subToneBaseDb_` is initialised to `SubharmonicEngine::kDefaultToneLevelDb` (`:1790-1791`). `applySubToneLevels` uses `subToneBaseDb_[t] + subToneOffsetDb_` (`:1366-1370`), and `prepare` calls it (`:402`). `VoragoEngine_SubToneLevelBase` passed. |
| FR-006 | pass | `vorago_engine.h:989-1013`: `setGhostReverseProbability` forwards to `atmos_.setGrainReverseProbability` and sets `ghostReverseSet_`. `setGhostEventTriggers` does `if (ghostEventTriggers_ && !on) ghostTriggerHigh_ = false;`. Getters and `isGhostTriggerLatchHigh()` exist. `prepare` only takes the config value when no setter was called: `if (!ghostTriggersSet_)` (`:342`) and `if (!ghostReverseSet_)` (`:375`). `VoragoEngine_NewSettersSurvivePrepare` (`SurvivesPrepare` and `DisarmAndRearm` sections) passed. |
| FR-007 | pass | `git diff --stat HEAD` shows the admitted dsp/ changes: `aether_reverb.h` (+`rebuildMatrixFromSeed` at `:2429-2435`), `cavern_verb.h` (+forwarder at `:768`), `resonance_drift_network.h` (+`setOctaveLock`, whose lock-0 branch is bit-equal), `vorago_engine.h`, `vorago_macro_matrix.h`, `vorago_voice.h` and `dsp/tests/CMakeLists.txt`, all admitted by FR-007/B-1/B-2/B-4. No existing dsp test file was edited; only 2 new files added under `dsp/tests/unit/systems`. `rebuildMatrixFromSeed` is called only from `plugins/vorago/src/processor/processor.cpp:716`. The `resonance_drift_network.h` consumers (bloom/ecosystem/feedback_ecology/subharmonic/vorago_voice) are not included by `plugins/seraphis`. `seraphis_tests` rebuilt and re-run: "All tests passed (444660 assertions in 109 test cases)" (excluding `[long]`/perf). Artifact `t057_dsp_effects_tests.log`: "All tests passed (122746 assertions in 499 test cases)". |
| FR-010 | pass | `plugins/vorago/src/plugin_ids.h`: `kCurrentStateVersion = 2` (`:22`). Enum holds every C-5 ID: global 0-5, macros 100-111, cloud 200-206, noise 300-302/310-353, resonance 400-403, ecology 500/501/510-515, sub 600/601/610-612, smear 700-702, events 800, ecosystem 900, body 1000-1005, space 1100-1115, envelope 1200-1206, bloom 1300-1301, ghost 1400-1403, life 1500-1502 — 108 IDs total. Reserved-map comment updated, 1600+ marked UNASSIGNED. 16 range-end constants (`kGlobalParamRangeEnd` through `kLifeParamRangeEnd`). |
| FR-011 | pass | `plugins/vorago/src/parameters/` has bloom, body, cloud, ecology, ecosystem, envelope, events, ghost, life, noise, resonance, smear, space and sub `_params.h`. Each defines `handle*ParamChange`, `register*Params`, `format*Param`, `save*Params`, `load*Params` (bool, EOF-safe) and `load*ParamsToController`, e.g. `body_params.h:67/102/139/163/177/205`. `global_params.h` is extended with `saveGlobalParamsV2Ext` at `:206`. `macro_params.h` is unmodified. The 15 `Vorago_*ParamsContract` tests passed. |
| FR-012 | pass | `body_params.h:118-129` registers an 11-entry dropdown in `BodyMaterial` declaration order (`continuous_body.h:84-97`). It pins `defaultNormalizedValue = indexToNormalized(defaultIndex, 11)`, and static_asserts `StoneChamber == 5` and `SteelTank == 6` (`:47-50`). Every `createDropdownParameterWithDefault` call has a matching `defaultNormalizedValue` pin in every pack. `Vorago_ParameterInfoTable` (SC-018) and `Vorago_ParamMapping` passed: `vorago_tests` "All tests passed (3660550 assertions in 42 test cases)". |
| FR-013 | pass | `processor.cpp:478-545` (`processParameterChanges`) takes the last point, skips `!Krate::DSP::detail::isFinite(value)`, applies `std::clamp(value, 0.0, 1.0)`, then dispatches by an `id < k*ParamRangeEnd` chain. An unowned in-band ID falls through the pack switch default. Log tapers: `noise_params.h:137/161`, `resonance_params.h:91`, `events_params.h:46`, `space_params.h:124`. Zero-floored IDs use `offsetLogFromNormalized`: `envelope_params.h:73` (eps 10 ms), `bloom_params.h:72` (eps 1e-4), `space_params.h:126`. `param_mapping.h:44-46` evaluates log of `(units + eps)` only. Growth duration 1206 uses plain log over `[1, 120]` s (`envelope_params.h:44-47`), no zero floor. `Vorago_ParamInputHygiene` passed. |
| FR-020 | pass | `processor.cpp` `process()`: `latchReleasePending_` is consumed first, then `forcePushPending_` -> `pushAllSurfaces(PresetLoad)` (`:293-301`). Then `pushGlobalParams`, `pushEngParams`, `pushCavernParams`, `pushVoiceParams` (generation-gated at `:638`), `pushMacroBases` (on change, `:807-817`), then `macros_.setMacros`, `apply`, and `applyCavernTargets` once (`:302-309`), all before the slice loop at `:345`. `Vorago_MacroPushOncePerProcess` passed. |
| FR-021 | pass | `processor.cpp:821-844` `buildMacroVector` copies 11 atomics unmodified and sets `m.pressure = std::clamp(pressure + channelPressure, 0.0f, 1.0f)`. `setMacros` is called once per `process()` (`:306`). `Vorago_ChannelPressure` passed. |
| FR-022 | pass | `processor.cpp:619-635`: `pushAllSurfaces` invalidates `mbValid_`, `engValid_` and `cvValid_`, bumps `voiceParamGeneration_` and sets `lastAppliedVpGen_` to the sentinel. Called directly from `setupProcessing` (`:213`, `Scope::Reprepared`, pushes immediately). `setState` only does `forcePushPending_.store(true, release)` (`:443`), which `process()` consumes with `exchange(acquire)` (`:299`). `Vorago_SetStateAfterPrepareReachesDsp` passed. |
| FR-023 | pass | `processor.cpp:707-719`: on a changed seed index it calls `engine_->setSeed`, `engine_->rewindIdleVoices()`, `cavern_->setSeed(cavernSeedFor(i))` and `cavern_->rebuildMatrixFromSeed()`. `setupProcessing` calls `engine_->setSeed` before `engine_->prepare` (`:184-188`). `rewindIdleVoices` is at `vorago_engine.h:626-635`, using `resetForRecovery` on configurable slots, allocation-free. `Vorago_SeedParameter` passed. |
| FR-024 | pass | `processor.cpp:398-403`: `getLatencySamples` returns engine + cavern latency, unchanged by any parameter path. `Vorago_LatencyIndependentOfParameters` (SC-017) passed in a run of 42 test cases. |
| FR-025 | pass | The push functions (`processor.cpp:637-844`) use only relaxed atomic loads, fixed `std::array` and member setters. `SustainLatch` uses `std::bitset<128>` (`sustain_latch.h:76-77`). Pedal points are a fixed array capped at `kMaxPedalPoints` (`:568-572`). `Vorago_FullSurfaceAutomationAllocFree` (SC-015) passed. |
| FR-030 | pass | `plugins/vorago/src/processor/sustain_latch.h:22-79` is a fixed-bitset latch with down->up release of latched, unheld notes (`:46-55`). `setState` makes no DSP call: it only runs `latchReleasePending_.store(true, release)` (`processor.cpp:444`). `process()` does `if (latchReleasePending_.exchange(false, acquire)) latch_.releaseAll(... engine_->noteOff)` (`:282-284`). Every pedal point is collected (`:553-579`) and interleaved in the slice loop (`:367-371`). `Vorago_SustainLatch` and `Vorago_SustainLatchUnit` passed. Minor stale comment: `clearWithoutRelease` is labelled '(setState path)' at `sustain_latch.h:66`, but is actually used by `setupProcessing`. |
| FR-031 | pass | `controller.h:28-30` inherits `IMidiMapping`, with `DEF_INTERFACE(IMidiMapping)` at `:52`. Header comment at `:9-13` updated; no `INoteExpressionController`. `controller.cpp:160-177` returns `kResultFalse` unless bus index 0, maps `kCtrlSustainOnOff` to `kSustainPedalId` and `kAfterTouch` to `kChannelPressureId`, `kResultFalse` otherwise. |
| FR-032 | pass | Channel pressure feeds only `m.pressure` (`processor.cpp:833-835`) and is read raw with no smoother. Not written by `saveGlobalParams` or `saveGlobalParamsV2Ext` (`global_params.h:176-180, :206-210`); `setState` resets it to 0 (`processor.cpp:440`). |
| FR-040 | pass | Gap closed. `processor.cpp:437-467`: for version >= 2 `setState` loads the v2 tail. The else branch writes the default-constructed packs' v2 tail into a stack `std::array<char, kStateV2Bytes-60>` `MemoryStream` and loads it through the same `loadV2Tail` chain; future versions still rejected (`:421-422`). `controller.cpp:130-155` mirrors this. Test `plugins/vorago/tests/unit/state_v2_test.cpp` SECTION `(2) v1 stream` (`:463`): a processor with every field dirtied loads v1 and `REQUIRE(after[k] == defaultSnap[k])` at `:523`, non-vacuity `REQUIRE(dirtiedOthers == 92u)` at `:525`; controller mirror `REQUIRE(ctlOthers == 92u)` at `:559`. `vorago_tests.exe "Vorago_StateRoundTripV2"`: "All tests passed (116101 assertions in 1 test case)". Build clean, exit 0, no warnings/errors. |
| FR-041 | pass | `controller.cpp:60-75` `initialize` registers global, macros and all 14 packs (108 IDs). `getParamStringByValue` (`:129-146`) tries all 16 `format*Param` functions and falls back to `EditControllerEx1` for StringLists. `Vorago_ParameterInfoTable` passed. |
| FR-045 | pass | `processor.cpp:438-440`: `setState` stores 0 to `globalParams_.sustainPedal` and `channelPressure`. Neither is serialized (`global_params.h:176-180, :206-210`; `kStateV2Bytes` breakdown has no entry for them). `Vorago_StateRoundTripV2` passed. |
| FR-050 | pass | Phase 12 test cases for SC-002..SC-023 exist in `vorago_tests` (45 cases) and `dsp_systems_tests` (`vorago_param_surface_test.cpp`, 13 cases). Phase 11 SC-023 section is inverted, not deleted: `param_flow_test.cpp` SECTION `'MacrosAreInert'` becomes `'MacrosAreLive'`, asserting getters and render differ. Runs: `vorago_tests` "All tests passed (3660550 assertions in 42 test cases)" (non-long); Phase 12 dsp cases "67563 assertions, 10 cases" pass. Artifact `t057_vorago_tests.log` reports 45 cases passing including `[long]`. |
| FR-051 | pass | Re-run this session: `pluginval --strictness-level 5` on the fresh `Vorago.vst3` exited 0. `run-clang-tidy.ps1 -Target vorago`: 38 files, Errors 0, Warnings 0. `-Target dsp`: 368 files, 0/0 (artifact `clang_tidy_dsp.log`). `check-portability` "all clear -- 32 compiled"; `lint-odr` OK; `lint-layers` OK; `lint-nonfinite-symbols` OK; `lint-float-bit-goldens` clean; `lint-plugin-roster` OK; all exit 0. The editor-lifecycle harness `Vorago_EditorLifecycle` passed. The editor-lifecycle harness under ASan: see the `asan-lifecycle.log` note below the table. |
| FR-052 | pass | `plugins/vorago/CLAUDE.md` has the ID table with new bands (`:30-46`), the route table (`:56-67`), state v2 with `kStateV2Bytes = 428` (`:68-88`), '1. OQ-7 ... DELIVERED in Phase 12 as IMidiMapping' (`:159-164`), '5. CC64 (sustain) — DELIVERED in Phase 12' (`:219-221`). Decision 3 rewritten as the 'Shipped mechanism (Phase 12)': `kOutputSaturationId` is an MB route via `setTargetBase` (`:195-206`). |
| FR-053 | pass | `processor.h:52` forward-declares `detail::VoragoMasterGainSmootherBypassProbe`, friend at `:152`. Flag `masterGainSnapProbe_ = false` (`:218`) is read at `processor.cpp:688` to `snapTo` instead of `setTarget`. The probe struct is defined only in `tests/integration/continuity_test.cpp:58-59`, so the shipped plugin never sets the flag (inert). No dsp seam added. Artifact `sc011_continuity.log`: "control (b): snapped master gain ... ratio=1.8768 -> fails clause 3 (OK)". |
| FR-060 | pass | `specs/vorago-phase12-parameters/compliance.md` (superseded by this document) had the FR-060/SC-021 table: Gravity endpoint 0.18014 -> retuned rho -0.9333 / 0.8058, `ResonanceOctaveLock` row, `vorago_macro_matrix.h:450-455`. Pressure rho -0.566667 / 0.0735783 dB -> retuned -1.0000 / 3.5217 dB, `OutputSaturation` 0.35->0.88 + `OutputDriveDb` +18 dB + `SubToneLevelOffsetDb` -12 dB (B-7), `:506-535`. Mass rho -1 / -0.446365 dB -> retuned 1.0000 / 2.4343 dB, `:751-756`. Each row cites ruling (b) (spec Clarifications Q1) and the R-1 path B probe logs. Re-run this session: `VoragoMacro_SweepAxes` printed "Gravity ... rho=-0.933333 endpoint=0.80581", "Pressure crest factor (dB) rho=-1 endpoint=3.52174 threshold=3", "Mass energy < 80 Hz (dB) @C3 rho=1 endpoint=2.43425 threshold=0.25", "All tests passed (38 assertions in 1 test case)". |
| FR-061 | pass | `plugins/vorago/src/parameters/param_routes.h:280-285` static_asserts route totals MB 39 + VP 31 + ENG 14 + CV 9 + MAC 13 + Local 2 = 108. `:288-292` asserts every `VoragoMacroTarget` is MB-routed or macro-only (the 2 new targets add no ID). The `plugin_ids.h` enum holds exactly the C-5 IDs (108). `Vorago_ParameterInfoTable` passed. |
| SC-001 | pass | Rebuilt `vorago_tests`, `dsp_systems_tests`, `dsp_effects_tests`, `shared_tests` and `Vorago`. Build exit=0, 0 warning lines. Suite runs: `vorago_tests` (excl. perf) "All tests passed (5293012 assertions in 45 test cases)"; `dsp_effects_tests` "All tests passed (122746 assertions in 499 test cases)"; `dsp_systems_tests` (excl. perf/probe/long) "All tests passed (6020500 assertions in 1399 test cases)"; `shared_tests` "All tests passed (6424 assertions in 461 test cases)". Every Phase 12 `[long]` DSP case run by name and passed. `pluginval` exit=0, 0 fail lines. `run-clang-tidy.ps1 -Target vorago`: "Files analyzed: 38, Errors: 0, Warnings: 0"; `-Target dsp`: "Files analyzed: 368, Errors: 0, Warnings: 0". All six node lints exit=0. |
| SC-002 | pass | `Vorago_Phase12DefaultsMatchPhase11Chain` (`param_surface_test.cpp:63`), run with `-s`: precondition `peakOf(refL.subspan(kLatencySamples)) >= 1e-4f` expands to `0.07799f`; R expands to `0.05486f`. `diffL <= 1e-5f` expands to `0.0f <= 0.00001f`, same for R. Reference built independently in `phase11_reference_chain.h:90-92, 125-126, 151`. "All tests passed (4515 assertions in 1 test case)". |
| SC-003 | pass | `Vorago_RegisteredDefaultsMatchEngine` (`param_surface_test.cpp:1788`) asserts `paramCount == 108` (`:1795`) and prints "SC-003 rows:" (`:1935`). `REQUIRE( rows == 97u )` expands to "97 == 97" (`:1936`). `PushIntegrity` section also reads "97 == 97" (`:2045`). "All tests passed (6204 assertions in 1 test case)". |
| SC-004 | pass | `Vorago_EveryRouteReachesTheChain` (`param_surface_test.cpp:1332`): "All tests passed (43013 assertions in 1 test case)". Sections MB 2.653s, VP 2.415s, ENG 0.066s, Cavern 25.620s. All 32 Cavern `diff[LR] <= 1.0e-6f` checks expand to 0.0f. B-5 non-vacuity smallest values 0.0013871036 / 0.0027240937 / 0.0033165408. `checked == 16u` gives 16==16, `cvRows == 9u` gives 9==9. Freeze read through `isFrozen()` at `:1723`. |
| SC-005 | pass | `Vorago_MacrosDriveTheMatrix` (`param_surface_test.cpp:280`): 13 arms. Clause (1): exact getter equality, all pass. Clause (2): all 26 `diff[LR] <= 1.0e-6f` asserts expand to 0.0f, RMS precondition up to 0.004935708 >= 0.0001. Clause (3): WARN "SC-005 (3) rmsDiff(defaults, all macros at 1) L=0.00458192 R=0.00313449" (0.0045819184 > 0.001). "All tests passed (35504 assertions in 1 test case)". |
| SC-006 | pass | `dsp_systems_tests "VoragoMacro_TargetBaseOverride"` (`vorago_param_surface_test.cpp:284`) on the rebuilt binary: "All tests passed (1287 assertions in 1 test case)", including clause (1) on the retuned `kRows`. |
| SC-007 | pass | `Vorago_MacroPushOncePerProcess` (`param_surface_test.cpp:559`). Counter section: `macroPushCountForTest() == before+1` for event counts {0,1,64,1024}. `BlockSizeInvariance`: reference (512) peak over [3072, end) = 0.000592015 (>= 1e-4). `maxAbsDiff` vs 512 for blocks 1, 7, 64, 65, 2048, 4096 is "L=0 R=0" (threshold 1e-5). `requirePedalScriptLatched`: `activeWhileLatched == 2`, `activeAfterPedalUp == 1`. The 4096 run asserts `eventFreeSliceCount == 2` (`:612`). |
| SC-008 | pass | `Vorago_StateRoundTripV2` (`state_v2_test.cpp:387`): "All tests passed (115903 assertions in 1 test case)". (1) atomic snapshots bit-identical, controller within 1e-9 (`:436`), render diffs 0.0f. (2) 60-byte v1 stream (`:463`), "others == 92u" (`:504`). (3) truncation at every offset, 0.211s. (4) future version gives `kResultFalse` for processor and controller (`:576, :581`). (5) NaN in each float field. (6) stream equals `kStateV2Bytes = 428` (`plugin_ids.h:28`) with sustain/pressure absent. |
| SC-009 | pass | `Vorago_SetStateAfterPrepareReachesDsp` (`param_surface_test.cpp:2157`): `probe.size() == 106`, 106 `REQUIRE_FALSE` non-default checks. Read-backs pass: 144 MB voice/engine target matches, 39 matrix bases, 36 loop filter modes, 24 comb fundamental/feedback/spread, 18 noise type/model. "All tests passed (772 assertions in 1 test case)". |
| SC-010 | pass | `Vorago_ParamInputHygiene` (`param_table_test.cpp:859`): unregistered set {6,99,112,207,399,516,613,703,801,901,1006,1116,1207,1302,1404,1503,1599,1600} (`:867-868`) leaves atomics unchanged. Non-finite and out-of-range: 58320 `isFinite(*v)` and 58320 `insideRange` checks all pass. "All tests passed (356598 assertions in 1 test case)". |
| SC-011 | pass | `Vorago_ParameterStepsAreContinuous` `[long]` (`continuity_test.cpp:293`), 386.358s: "SC-011 continuity (B-6 geometry): 108 IDs, 64 steps every 13824 samples ... bound 1.50 x max(ref), ceiling 0.9661". 108 rows, 0 non-PASS. Worst ratio ID 2 at 1.4903 (class C, only clause 4 applies). Channel pressure (ID 5) 0.8958. "failing IDs (hand to T048): none". Control (a): "own=0.00194047 injected=0.00534437 bound=0.00291071 -> exceeds (OK)". Control (b): "snapped master gain ... ratio=1.8768 -> fails clause 3 (OK)". |
| SC-012 | pass | `Vorago_SustainLatch` (`sustain_test.cpp:339`), normal run: "All tests passed (9788 assertions in 1 test case)". Sections cover pedal-down/up latching, re-strike survival, `setActive(false)` immediate release, `setState` release on next process only, and a 10000-event random fuzz with 0 allocations (`:565`). |
| SC-013 | pass | `Vorago_ChannelPressure` (`channel_pressure_test.cpp:209`): sections MidiMapping, PressureZeroMacroVectorEqualsAtomics, FullPressureAtDefaultKnobEqualsKnobOne, ComposesAdditively, SumIsClampedAtOne, SetStateZeroesSustainAndPressureUntilNextProcess. 131 `withinTol(a.targets, b.targets)` pass. "All tests passed (424 assertions in 1 test case)". Clause (6): ID 5 in SC-011 class A, ratio 0.8958 PASS. |
| SC-014 | pass | `Vorago_SeedParameter` (`seed_test.cpp:95`): (2)/(4) "maxAbsDiff L=0 R=0" against 1e-5. (5) LiveReseed: allocs == 0, peak <= ceiling. "All tests passed (21083 assertions in 1 test case)". (3) `Vorago_SeedTableSpread` `[long]`: "pairs=120 failures=0 min rmsDiff L=0.0108607 R=0.00885332 (gate > 0.001)". |
| SC-015 | pass | `Vorago_FullSurfaceAutomationAllocFree` (`automation_rt_test.cpp:45`), normal run: "All tests passed (10013 assertions in 1 test case)". Asserts `totalAllocs == 0` (`:136`), finite (`:137`), `peak <= kOutputCeiling` (`:138`), `getNonFiniteRecoveryCount() == 0` (`:142`), `noteOns > 1000` (`:146`). Measured peak=0.54797, noteOns=2512, pedalPoints=4000. |
| SC-016 | pass | `Vorago_ProcessorCpu` `[.perf]`, always alone and P-core pinned, three isolated readings on 2026-09-25/26: run 1 (`artifacts/sc016_cpu_full.log`, via `node tools/run-cpu-tests.js vorago_tests`, 15:51, warm machine): arm P 4.1525e+06, arm D 3.95014e+06, P/D **1.05123 > 1.05 FAILED** (`REQUIRE( pBestNs <= kWrapperOverheadCeiling * dBestNs )` expands to `4152505.0 <= 4147650.15`); run 2 (`artifacts/sc016_cpu_run2.log`, single case after 16 min idle, 16:20): P 3.81693e+06, D 4.15646e+06, P/D **0.918314 PASS**, arm E 2048-block 0.378872 of real time; run 3 (`artifacts/sc016_cpu_run3.log`, the runner again at 00:32 on a cold machine): P 2.43897e+06, D 2.41369e+06, P/D **1.01047 PASS**, P / kReferenceNs 0.762178, arm A P_A/D 0.967286, arm E 1.14442e+07 ns (Phase 11 recorded 1.79691e+07, ratio 0.636882). The verdict flipped with no code change between runs 1 and 2, which the repo protocol reads as the machine, not the code; the gate passes on both isolated re-runs after idle. Recorded, not absorbed. |
| SC-017 | pass | `Vorago_LatencyIndependentOfParameters` (`param_surface_test.cpp:2108`): 927 checks `getLatencySamples() == kLatencySamples` expand to "3072 (0xc00) == 3072 (0xc00)". 3 sample-rate arms, each "checked == steps" 307==307. "All tests passed (6480 assertions in 1 test case)". |
| SC-018 | pass | `Vorago_ParameterInfoTable` (`param_table_test.cpp:607`): `getParameterCount() == 108` and `kExpectedParams.size() == 108` (`:614-615`). Every title/unit/stepCount/flag/default checked: 107 default checks show "0.0 <= 0.000000001". 324 non-empty `getParamStringByValue` results at 0/0.5/1. 86 `closeRel(packMid, row.mid, 1e-9)` taper checks. "All tests passed (2409 assertions in 1 test case)". |
| SC-019 | pass | 60s `[long]` arms: `VoragoEngine_NewSettersDefaultInert` "max-abs diff L 0 R 0" (1e-6 bound); `VoragoEngine_ApplyVoiceParamsDefaultIsNoOp` "All tests passed (188 assertions)". `git diff --stat -- dsp/tests` shows only `dsp/tests/CMakeLists.txt` (+16 lines registering 2 new TUs); no existing test edited. `dsp_effects_tests`: 499/499 pass. `dsp_systems_tests` (non-long): 1399 cases pass. |
| SC-020 | pass | `Vorago_RandomSurfaceSoak` `[long]` (`soak_test.cpp:129`), 90.296s: "R_default=0.0566337". 5 seeds (12020-12024), 12 redraws each, all finite. Peaks 0.447349/0.201738/0.224446/0.173434/0.187182, all at or under 0.9661. Last-10s RMS -2.29/-11.36/-7.64/-22.86/-19.90 dB relative to R_default, against a -60 dB floor. Test passed. |
| SC-021 | pass | Axes (`artifacts/sc021_sweepaxes.log`, `VoragoMacro_SweepAxes` alone, 2026-09-25): "All tests passed (38 assertions in 1 test case)" — Gravity rho −0.9333 endpoint **0.8058** (≥ 0.30; Phase 10 0.18014; T004 best existing-target 0.2024; spec B-1 `ResonanceOctaveLock`), Pressure rho −1.0 endpoint **3.5217 dB** (≥ 3; Phase 10 0.0735783; T004 best 1.5030; Q10 drive alone 2.6003 at +24; spec B-2/B-7 drive +18 dB + sub −12 dB), Mass rho 1.0 endpoint **2.4343 dB** (≥ 0.25; Phase 10 −0.446365; spec Q9 +3.0 dB row); the other nine axes unchanged from Phase 10. Depth (`artifacts/sc021_depth_axis.log`, `VoragoComposed_DepthMacroAxis` alone): "All tests passed (5 assertions in 1 test case)". SC-002 unchanged (no base moved; `VoragoMacro_TargetBaseOverride` and the T036 guard green). CPU gate `VoragoEngine_CpuBudget` (alone, pinned): `artifacts/sc021_cpu.log` (15:34, after 16 min idle) engine 3.89004e+06 / +Cavern 4.01454e+06 ns/block **> 3.2e+06 (125.5 %) FAILED clause (i)**, clause (ii) passed; same-conditions A/B on the untouched pre-phase binary `artifacts/sc021_cpu_base_f149cced.log` (15:17): 4.23711e+06 / 4.36161e+06 (136.3 %) — Phase 12 is 8 % faster than the baseline, no regression (spec B-8, recorded machine-limited); the overnight cold-machine re-run `artifacts/sc021_cpu_rerun.log` (00:47, single case pinned): **2.78512e+06 <= 3.2e+06 (87.0348 %) PASSED**, within 2 % of Phase 10's own 2.74039e+06. Thresholds untouched. |
| SC-022 | pass | `dsp_systems_tests "VoragoEngine_NewSettersSurvivePrepare"` (`vorago_param_surface_test.cpp:1308`) on the rebuilt binary: "All tests passed (19 assertions in 1 test case)". |
| SC-023 | pass | (1) `VoragoEngine_RepeatedBroadcastIsInert` `[long]`, 10s: "A vs B max-abs diff L 0 R 0" (<= 1e-5). B-3 positive control "rmsAC > 1e-3" expands to "0.0476673832 > 0.001". "All tests passed (4 assertions)". (2) `Vorago_HostResendsEveryParameter` (`param_surface_test.cpp:2052`): peaks 0.1022f/0.07055f (>= 1e-4); diffs both expand to 0.0f. "All tests passed (11387 assertions in 1 test case)". |
| CC-rt | pass | Every new process-path function read in the working-tree diff: `process()` calls, in order, `pushAllSurfaces` (when `forcePushPending_`), then `pushGlobalParams`, `pushEngParams`, `pushCavernParams`, `pushVoiceParams`, `pushMacroBases`, `macros_.setMacros(buildMacroVector())`, `macros_.apply` and `applyCavernTargets`, using only relaxed atomic loads, fixed `std::array` trackers and a static constexpr function-pointer table. Pedal points are a fixed array capped at `kMaxPedalPoints=128`. `setState()` makes no DSP call. New DSP entry points (`VoragoEngine::rewindIdleVoices`, `applyVoiceParams`, `AetherReverb::rebuildMatrixFromSeed`, `CavernVerb::rebuildMatrixFromSeed`, `ResonanceDriftNetwork::setOctaveLock`) are all `noexcept` and use only bounded stack scratch (`kAetherMaxMatrixOrder=16`) or member arrays. `SustainLatch` holds two `std::bitset<128>` plus a bool, all methods `noexcept`. A grep of the whole diff for `new`, `make_unique`, `vector`, `string`, `resize`, `push_back`, `reserve`, `throw`, `mutex`, `lock_guard`, `printf`, `fopen`, `cout`, `malloc` found only a `snprintf` in a controller-side to-string function (not audio thread). Minor notes: `sustain_latch.h:49/60` calls `std::bitset::test()` (unreachable throw path, guaranteed in-range); `MatrixMorph::build`'s doc comment still says "prepare()-time only" though it now also runs live on a seed change (no allocation, O(n^3) once per change). |
| CC-layers | pass | `node tools/lint-layers.js`: "lint-layers: OK — no layer-dependency violations in 5-layer DSP tree." (exit 0). Manual include check: `vorago_engine.h`, `vorago_macro_matrix.h`, `vorago_voice.h`, `resonance_drift_network.h` include only downward layers; `effects/cavern_verb.h` includes the same-layer `effects/aether_reverb.h` (permitted). New plugin headers (`parameters/*.h`, `processor/sustain_latch.h`) include only `plugin_ids.h`, `parameters/*`, shared UI helpers, VST3 SDK headers, krate DSP headers and stdlib. A grep of `processor.h`/`processor.cpp` for controller/vstgui/editor includes found nothing. |
| CC-naming | pass | `node tools/lint-odr.js`: "lint-odr: OK — 800 definitions scanned, no cross-file name collisions." New types are PascalCase inside `namespace Vorago` (`CloudParams`, `NoiseParams`, ... `SustainLatch`, plus `Krate::DSP::VoragoVoiceParams`). New Processor members carry a trailing underscore. Constants use `kPascalCase`. Functions are camelCase. `plugin_ids.h`: 108 enumerators match `k[A-Z]...Id`. A grep for the forbidden names `DryWet` and `Modulation...Id` found none. |
| CC-warnings | pass | A forced rebuild (touch every modified/untracked `.h`/`.cpp`, then `--target dsp_systems_tests vorago_tests Vorago`) compiled 48 TUs with `grep -i warning` giving 0 hits. One transient `LNK1104` error was traced to two parallel-agent `vorago_tests.exe` processes holding the file lock (environmental, not a compile diagnostic) and resolved by a separate clean `--target Vorago` build (exit 0, 0 warnings, `Vorago.vst3` produced). `dsp_effects_tests` was not part of the forced-touch rebuild in that pass, but its `aether_reverb.h`/`cavern_verb.h` changes compiled warning-free through the Vorago plugin and vorago test TUs that include them, and a later full T055/T057 build+run cycle rebuilt and passed `dsp_effects_tests` cleanly (see SC-001/SC-019/CC-portability rows). |
| CC-portability | pass | `node tools/check-portability.js` (exit 0): "check-portability: all clear -- 32 compiled." for every changed TU including `processor.cpp`, `controller.cpp`, all 17 `params/*_test.cpp` files, `state_v2_test.cpp`, `automation_rt_test.cpp`, `continuity_test.cpp`, `seed_test.cpp`, `soak_test.cpp`, `sustain_test.cpp`. Supporting gates: `lint-nonfinite-symbols` "all clear (17 guarded files)"; `lint-simd-aligned-loadstore` "clean -- 1619 file(s) scanned"; `lint-float-bit-goldens` "clean (1619 files scanned)". New non-finite checks use `Krate::DSP::detail::isFinite` (bit-pattern), never `std::isnan`. Brace initializers use designated initializers. |

## Implementation notes — deviations reported by task agents

- **FR-060 retune path (T004-T007).** The T004 probe (`artifacts/fr060_probe.log`) showed no admissible row on the 39 Phase 10 targets reached threshold for Gravity or Pressure, so both went down the R-1 contingent path B (spec Q9, B-1/B-2/B-7): two new macro-only targets were added, `ResonanceOctaveLock` (Gravity) and `OutputDriveDb` (Pressure, makeup-compensated), plus a second Pressure row onto `SubToneLevelOffsetDb` (B-7). Mass was retuned in place with a +3.0 dB row (T006), the smallest amount that passed. `VoragoMacroTarget::Count` moved from 39 to 41; `kNumRows` moved 46 -> 47 (T006) -> 49 (T007) -> 50 (later probe iteration). This is a decision-tree resolution recorded in spec.md, not a silent scope change.
- **T005 was initially blocked** (probe log did not yet exist when T005 ran) and **T047 was initially blocked** (the SC-011 positive control (b), a snapped master-gain step, could not fail clause 3 given the window's placement relative to output-stage vs. engine-path latency). Both were resolved by later tasks: T007 supplied the probe data T005 needed, and the final `Vorago_ParameterStepsAreContinuous` run (T048's log, `artifacts/sc011_continuity.log`) shows control (b) failing clause 3 as intended and zero failing IDs. Neither blocker survives in the final evidence above.
- **T009**: the spec's literal wording ("every getter on all six slots equals p after 4800 samples") cannot hold for the noise model/effective-type getters on slots that never render (idle slots only get `advanceLifeOnly`, never touching the noise organism). The test was split: all other fields checked on all six slots after 4800 samples, then polyphony raised to `kMaxVoices` with four notes added and a second 4800-sample check covering model/type on all slots.
- **T014**: `kBodyMixId` collides in spelling (not namespace) with an existing Innexus ID; different namespace, no ODR conflict, flagged in review as required.
- **T024/T027/T033**: three registered defaults (Body Blend, Body Resonance, Bloom Depth) were initially computed from a `float` literal widened to `double`, landing 6e-9 to 2.4e-8 off the double literal in `plan.md` — enough to fail the table's 1e-9 margin. The fix (registering the double literal directly) was applied before final compliance.
- **T037**: Ecology Loop Gain's default already sits at normalized 0.8; that row's non-vacuity probe uses 0.2 instead, with the substitution documented in the test.
- **T039/T041**: the ENG-route push order intentionally left `engValid_`/`voiceParamGeneration_` reset out of `pushEngParams`/`pushVoiceParams` themselves; T041's `pushAllSurfaces` is the single place that forces a full re-push on re-prepare or preset load (D-P2: seed and polyphony trackers are deliberately excluded from that invalidation).
- **T047**: window/step spacing changed from the spec's 125 ms to 128 ms (6144 samples) so every step lands on a 512-sample block boundary, keeping windows clear of adjacent steps by the required >=50 ms margin; class-A stepping starts at normalized 0 rather than the default index so every step is the same size.
- **T052**: the SC-016 "R-5 remedy arm" is recorded as not applicable — T048 found zero failing continuity IDs, so no remedy was needed; the arm is logged as such rather than fabricated.
- **T053/SC-021**: the CPU budget gate (`VoragoEngine_CpuBudget`) read red once on a warm/loaded machine (4.01e6 ns vs 3.2e6 reference, 125.5%) and green on a cold re-measure (2.78e6 ns, 87.0%), against a pre-phase baseline measured the same way. Per ruling B-8 this is recorded as machine-limited, not a regression, with the cold re-measure as the follow-up evidence (both logs are cited in the SC-021 row above).
- **T056**: one stale placeholder-style comment header remains in `plugins/vorago/tests/integration/seed_test.cpp:4` ("PLACEHOLDER registered by T002..."); the file's content is fully implemented, only the wording is stale. Flagged, not fixed (outside that task's file list).
- **T058**: the local clang-tidy compile database was stale (predated the Phase 12 TUs); regenerating it (configure-only, no build) surfaced the real warning set. All fixes landed in test files only — no plugin or DSP source changed. Final: vorago 38 files / 0 / 0; dsp 368 files / 0 / 0.
- **Housekeeping**: a stray empty untracked file named `off` sits in the repo root (noted under FR-051/SC-001) and should be deleted; not part of this phase's deliverable.

## Remaining gates for the human loop

- **clang-tidy**: already run clean this session (`vorago`: 38 files, 0 errors/0 warnings; `dsp`: 368 files, 0 errors/0 warnings — see FR-051/SC-001/CC rows). Re-run once more immediately before commit if any further edits land.
- **pluginval**: plugin code changed in this phase (Phase 11+ plugin scaffold), so the gate applies. Already run clean this session at `--strictness-level 5` against the freshly built `Vorago.vst3` (exit 0, 0 fail lines — see FR-051/SC-001 rows). Re-run if the plugin binary changes again before commit.
- **Commit**: not yet done. Pending human review of this report and the stray `off` file cleanup, then a commit per the project's no-amend / new-commit policy.

## Main-loop closure additions (2026-09-26)

Verified by the main loop after the workflow report; each line names the artifact log and the row it supports.

- **FR-051 editor-lifecycle harness under ASan** — `artifacts/asan-build.log` (`build-asan`, Debug, `ENABLE_ASAN=ON`, `vorago_tests` target: "build exit=0", 0 warnings) and `artifacts/asan-lifecycle.log` (`Vorago_EditorLifecycle`: "All tests passed (37 assertions in 1 test case)", exit 0, no AddressSanitizer report). The non-ASan run of the same case inside `vorago_tests` proves nothing about lifetime bugs; this run does.
- **FR-051 pluginval** — `artifacts/pluginval.log`: strictness 5 on the freshly built `Vorago.vst3`, "Completed tests in pluginval", exit 0.
- **FR-051 clang-tidy** — `artifacts/clang_tidy_vorago.log` ("[OK] Errors: 0", "[OK] Warnings: 0") alongside the already-cited `clang_tidy_dsp.log`.
- **FR-051 lints and portability** — `artifacts/lints.log`: the nine `lint-*.js` gates, the three generated-index checks, `check-portability.js` ("all clear", every Vorago TU compiled with g++ inside WSL) all exit 0; WSL was shut down afterwards (`wsl --shutdown`).
- **FR-007 DSP closure** — `artifacts/dsp_closure.log` (T013): "VERDICT: PASS", build 0 warnings, `dsp_systems_tests` 1402/1402 cases, `dsp_effects_tests` 495/495, `git diff f149cced -- dsp/tests/unit/` empty (no pre-existing DSP test edited), and the full-roster re-runs `artifacts/t057_dsp_systems_tests.log` ("All tests passed (6020500 assertions in 1399 test cases)") and `artifacts/t057_shared_tests.log` ("All tests passed (6420 assertions in 460 test cases)").
- **SC-019 [long] DSP cases** — `artifacts/sc019_dsp_long_apply_voice_params.log` (`VoragoEngine_ApplyVoiceParamsDefaultIsNoOp`: max-abs diff L 0 R 0), `artifacts/sc019_dsp_long_new_setters.log` (`VoragoEngine_NewSettersDefaultInert`: L 0 R 0), `artifacts/sc019_dsp_long_repeated_broadcast.log` (`VoragoEngine_RepeatedBroadcastIsInert`, 10 s: A vs B max-abs 0 / 0, control 0.0476673832 > 1e-3).
- **FR-060 / SC-021 Pressure amount (spec Q10, B-7)** — `artifacts/fr060_drive_probe.log`: drive alone at 6 / 12 / 18 / 24 dB reads 1.1654 / 2.0188 / 2.4876 / 2.6003 dB (all < 3); with the sub-level row, +18 dB / −12 dB reads **3.5217 dB** (the smallest passing set, landed), up to +24 / −24 at 4.1278.
- **Not kept**: the workflow's bookkeeping logs (`baseline.log`, `gate_build.log`, `gate_run.log`, `t057_build.log`, the runner summary `sc016_cpu.log` whose full text is `sc016_cpu_full.log`, and `sc021_summary.log`, superseded by the SC-021 row above) were deleted so that `artifacts/` holds exactly the files this document cites.
