# Compliance Report — Vorago Phase 1 (vorago-phase1-events-modulation)

**Spec slug:** `vorago-phase1-events-modulation`
**Overall status:** COMPLETE

All 82 compliance items below (functional requirements, success criteria — both the implementer's own pass and the independent re-verification pass — and cross-cutting constraints) returned verdict `pass`, each with cited evidence checked against real file:line references and actual test/tool output. Zero fail/partial items.

## Compliance Table

82 items. Two independent lenses are included for every SC-xxx criterion: `fr` rows are the implementer/build-agent's own pass; `sc` rows are a second, independently re-run verification pass (re-executed suites, re-derived numbers). `constraints` rows are the cross-cutting (CC-*) gates: real-time safety, layering/ODR, naming, compiler warnings, and portability.

| ID | Lens | Verdict | Evidence |
|---|---|---|---|
| FR-001 | fr | PASS | dsp/include/krate/dsp/processors/perlin_noise_source.h:174 `class PerlinNoiseSource : public ModulationSource` with `getCurrentValue() const noexcept override` :321 and `getSourceRange() const noexcept override` :326; dsp/include/krate/dsp/processors/slow_event_scheduler.h:133 `class SlowEventScheduler : public ModulationSource`, overrides at :321 and :328. Both asserted through a base handle (static_assert + `ModulationSource& ms = s`) in slow_event_scheduler_test.cpp:1079,1093 and perlin_noise_source_test.cpp:311,323 — both cases green in the full run. |
| FR-002 | fr | PASS | perlin_noise_source.h:211-217 `prepare(double)` floors at `sampleRate > 1.0 ? sampleRate : 1.0` (:212) then `initState()` (:216); `reset()` :221-223 calls the same `initState()`. slow_event_scheduler.h:197-200 identical 1 Hz floor + `initState()`, `reset()` :204 → `initState()`. Verified by test sections 'prepare() twice leaves no half-completed state' and 'prepare(0)/prepare(-1) hit the 1 Hz floor' (perlin_noise_source_test.cpp EdgeCases) and the prepare-mid-event block at slow_event_scheduler_test.cpp:1181-1200 — all green. |
| FR-003 | fr | PASS | perlin_noise_source.h:272-282 `process()` decrements `samplesUntilControl_` and calls `advanceControlStep()` every 32; :291-315 `processBlock()` chunks by `samplesUntilControl_` (`processBlock(0)` falls through the `while (remaining > 0)` guard). slow_event_scheduler.h:282-311 same construction with onset detection on the 32-grid only. Equivalence measured: perlin_noise_source_test.cpp:638 renders the non-aligned sequence {37,1,64,512} against a pure process() render (SC-004(a), green); 'processBlock(0) is a no-op' sections green in both TUs. |
| FR-004 | fr | PASS | Every method in both headers is `noexcept` — a grep for method definitions lacking `noexcept` in perlin_noise_source.h and slow_event_scheduler.h returns nothing; grep for `std::function\|new \|malloc\|throw` finds only two comment lines (slow_event_scheduler.h:393, perlin_noise_source.h:142). Measured zero allocations: PerlinNoiseSource_NoAllocInProcess, SlowEventScheduler_NoAllocInProcess (REQUIRE(allocations == 0u), slow_event_scheduler_test.cpp:935) and ChaosModSource_AizawaNoAllocInProcess (chaos_mod_source_aizawa_test.cpp:467) all green. |
| FR-005 | fr | PASS | perlin_noise_source.h:233-236 `setSeed()` stores `configuredSeed_` and derives four octave seeds via `deriveStreamSeed(base, k)` (:383) — no RNG object is a member (state block :466-480). slow_event_scheduler.h:219-222 `setSeed()` + `Xorshift32 rng_{kDefaultEventSeed}` :562; `initState()` :510 re-seeds. Determinism measured by PerlinNoiseSource_SeededDeterminism and SlowEventScheduler_SeededDeterminism (bit-identical vectors over 400 captured blocks) — both green. |
| FR-006 | fr | PASS | perlin_noise_source.h:326-328 and slow_event_scheduler.h:328-330 both return the literal `{-1.0f, 1.0f}` independent of depth. Non-tautology measured: SC-001's exact half-scaling `\|peak[0.5]/peak[1.0] - 0.5\| < 1e-4` (perlin_noise_source_test.cpp:412) green, and SC-011 depth peaks A=0.499138 B=0.998277 (ratio 0.50000) from the run log — a clamp cannot produce exact halving. |
| FR-007 | fr | PASS | perlin_noise_source.h:469 `double positionCells_` documented UNWRAPPED, kIndexBias at :354 makes negative indices well-defined; slow_event_scheduler.h:550 `double elapsedSamples_` with the carry at :499. Enforcing SC-017 (VoragoPhase1_LongRunResolution, 8 h) green: measured rms h1=0.301585 h8=0.285591 (5.3%), zcr h1=0.000195 h8=0.00019963 at kDefaultRate; scheduler events h1=58 h8=62, finalPeriod=24.6843 s. |
| FR-011 | fr | PASS | perlin_noise_source.h:415-424 `rawOctaveNoise`: `s = t*t*t*(t*((t*6.0)-15.0)+10.0)` (:419), gradient dot products `((g0*t)*(1.0-s)) + ((g1*(t-1.0))*s)` (:422), `std::int64_t i0` from `floor(x)` (:417); unit gradients ±1 from `deriveStreamSeed(octaveSeeds_[k], salt) & 1u` at :407-411 with `kIndexBias = 1LL << 31` (:354). |
| FR-012 | fr | PASS | perlin_noise_source.h:407-411 `gradientAt()` is a pure stateless hash of (octave seed, lattice index) — no RNG member exists in the state block (:466-480). Path-independence measured by SC-004(a) non-aligned block sequence {37,1,64,512} vs pure process() (perlin_noise_source_test.cpp:638) and SC-004(b) cross-rate identity — both green. |
| FR-013 | fr | PASS | perlin_noise_source.h:177-179 `kMinRate = 0.005f`, `kMaxRate = 5.0f`; :242-245 `setRate()` routes through `sanitizeClamp(cellsPerSecond, kMinRate, kMaxRate)`; getter `getRate()` :260. EdgeCases section 'setRate clamps, NaN maps to kMinRate' green. |
| FR-014 | fr | PASS | perlin_noise_source.h:249-252 `setOctaves()` = `std::clamp(numOctaves, kMinOctaves, kMaxOctaves)` with kMinOctaves=1/kMaxOctaves=4 (:181,183); `kPersistence = 0.5f` :185, `kLacunarity = 2.0f` :187; `amplitudeSumFor()` :389-397 yields 1/1.5/1.75/1.875 and `evaluateFbm()` divides by it at :437. Section 'setOctaves clamps to [1, 4]' green. |
| FR-015 | fr | PASS | perlin_noise_source.h:379-386 `deriveSeeds()` derives ALL kMaxOctaves streams via `deriveStreamSeed(base, k)` regardless of octaves_; `getOctaveValue()` :341-347 returns `rawOctaveNoise(k, position*2^k)` — excludes amplitude, 1/Σaₖ, depth and the smoother, returns 0 for out-of-range index. SC-004(c) 'octave-stream identity across octave counts' (perlin_noise_source_test.cpp:677) asserts bit-identity between setOctaves(1) and setOctaves(4) — green. |
| FR-016 | fr | PASS | perlin_noise_source.h:256-258 `setDepth()` = `sanitizeClamp(normalized, 0.0f, 1.0f)`; depth multiplies inside `evaluateFbm()` :437. Depth-0 exactness measured twice: SC-001's `REQUIRE_FALSE(anyNonZero)` at every corner (perlin_noise_source_test.cpp:396) and the EdgeCases 'setDepth(0) is exactly 0 for all time' 200 000-control-step render — both green. |
| FR-017 | fr | PASS | perlin_noise_source.h:43-55 derives \|n(x)\| ≤ 0.5 at t=0.5 with opposing gradients and states kGradientNormalize = 2.0 maps one octave to exactly [-1,+1]; constant at :189; terminal `std::clamp` in `evaluateFbm()` :438 documented inert at :52-55. Inertness measured by SC-001's exact half-scaling clause (green). |
| FR-018 | fr | PASS | perlin_noise_source.h:191 `kOutputSmoothMs = 5.0f`, configured unconditionally in prepare() at :214-215, advanced one OnePoleSmoother::process() per sample (:281, :310-312). Mechanism note: the smoother is advanced in process()/processBlock() and read in getCurrentValue() rather than applied inside getCurrentValue() as the FR's prose says — substance (mandatory 5 ms one-pole, step suppression) is unchanged and stricter. Measured: SC-002 two-sided band green (measured ∈ [0.5·predicted, predicted], predicted 8.118e-4 at n=4) and SC-003(c) absolute floor fracAbove(40 Hz) ≥ 1.30e-4 green — the clause a 20 ms smoother fails. |
| FR-019 | fr | PASS | perlin_noise_source.h:195-198 `kDefaultRate = 0.1f`, `kDefaultOctaves = 2`, `kDefaultDepth = 1.0f`, `kDefaultPerlinSeed = 0x9E37u`; in force from the member initialisers :467-479. Asserted after default construction AND after prepare() with no configuration call in PerlinNoiseSource_SharedContract (perlin_noise_source_test.cpp:315,328) — green. |
| FR-031 | fr | PASS | dsp/include/krate/dsp/primitives/chaos_waveshaper.h:60 `Aizawa = 4` appended after `Henon = 3,` (:56) — no existing index moved. ChaosModSource_AizawaNoRegression asserts Lorenz==0…Aizawa==4 (chaos_mod_source_aizawa_test.cpp:414-418) and chaos_waveshaper_test.cpp passes unmodified: dsp_primitives_tests 'All tests passed (4549290 assertions in 1505 test cases)'. |
| FR-032 | fr | PASS | dsp/include/krate/dsp/processors/chaos_mod_source.h:303-328 `updateAizawa(float dt)` with a=0.95,b=0.7,c=0.6,d=3.5,e=0.25,f=0.1 and `dx=(z-b)*x-d*y`, `dy=d*x+(z-b)*y`, `dz=c+a*z-(z*z*z)/3-(x*x+y*y)*(1+e*z)+f*z*(x*x*x)`, all read from the same pre-update state; forward-Euler `state_.x += dx*dt` etc. New arms in all three switches: :201 (updateModelParams), :237 (resetModelState), :274-275 (updateAttractor). |
| FR-033 | fr | PASS | chaos_mod_source.h:52 `kAizawaScale = 1.5f`, :211 `baseDt_ = 5.0e-4f`, :218 `safeBound_ = 25.0f`, :237-238 initial state `{0.1f, 0.0f, 0.0f}`; output still passes through the untouched tanh/clamp path. Non-collapse measured by SC-006(b): secondHalfMaxAbs ∈ (0.5, 0.99) and stddev > 0.1 at all five speeds (chaos_mod_source_aizawa_test.cpp:262-264, green), and getDivergenceResetCount()==0 at coupling 1.0 (:348, green). |
| FR-034 | fr | PASS | chaos_waveshaper.h:649 and :699 add grouped `case ChaosModel::Aizawa:` arms sharing the Lorenz behaviour in updateAttractor() and resetModelState(); the validator at :451-455 is untouched (git diff shows no change there) so setModel(Aizawa) still falls back to Lorenz — asserted at chaos_mod_source_aizawa_test.cpp:426,433-435. Exhaustiveness proven on the toolchains that can see it: `g++ -std=c++20 -Wall -Wextra -Wswitch -Werror -fsyntax-only … dsp/tests/portability/chaos_enum_exhaustiveness.cpp` exit 0 and the same clang++ invocation exit 0. |
| FR-035 | fr | PASS | `git status --short` lists only chaos_waveshaper.h and chaos_mod_source.h as modified headers under dsp/include — modulation_engine.h is untouched, so setChaosModel/getChaosModel forward Aizawa with no engine edit. dsp_systems_tests unaffected; dsp_processors_tests full default run green (3318 cases). |
| FR-036 | fr | PASS | chaos_mod_source.h:168-170 `[[nodiscard]] std::uint32_t getDivergenceResetCount() const noexcept`, member at :397, incremented before re-seeding inside checkAndResetIfDiverged() at :387, zeroed only by prepare() (:66) and reset() (:74). Behaviour pinned by ChaosModSource_DivergenceCounterObservable (chaos_mod_source_aizawa_test.cpp:374-403: 0 initially, >0 after a forced divergence, 0 again after reset/prepare) — green. |
| FR-051 | fr | PASS | dsp/include/krate/dsp/processors/slow_event_scheduler.h:133 declares the class in the Layer 2 processors directory including only Layer 0 headers (db_utils.h, modulation_source.h, random.h at :115-117). `node tools/lint-layers.js` → 'OK — no layer-dependency violations in 5-layer DSP tree.' |
| FR-052 | fr | PASS | slow_event_scheduler.h:228-233 `setIntervalRange()` clamps both bounds to [kMinIntervalSeconds=1, kMaxIntervalSeconds=600] (:143-144) and raises hi to lo when inverted (so (90,20) → fixed 90 s, verified in the EdgeCases block at slow_event_scheduler_test.cpp:1218); defaults kDefaultMinInterval=20/kDefaultMaxInterval=90 (:150-151); uniform draw `lo + u*(hi-lo)` from `rng_.nextUnipolar()` at :431-436; kDefaultEventSeed = 0x51E7u :159. Measured over 16 000 draws: zero out-of-range, mean 54.8526 s, worst histogram deviation 3.875 %. |
| FR-053 | fr | PASS | slow_event_scheduler.h:177-181 nested POD `struct Event { std::uint8_t target; float depth; std::int8_t polarity; }` — not named ScheduledEvent (documented at :172-176); single `Event active_{}` member at :543. Single-active-event holds by construction because fitSegments() (:442-456) caps releaseEndSamples_ at minIntervalSeconds_ ≤ the drawn period. `node tools/lint-odr.js` → 'OK — 738 definitions scanned, no cross-file name collisions.' |
| FR-054 | fr | PASS | slow_event_scheduler.h:497-503 `takeOnsetIfDue()` fires when `elapsedSamples_ >= periodSamples_` and subtracts the period — the drawn period is measured onset-to-onset. SC-008(d) measures the identity directly: with setIntervalRange(30,30) every getPeriodSeconds()==30.0 and every rising-edge spacing is within one control step of 30 s (slow_event_scheduler_test.cpp:335-347) — green. |
| FR-055 | fr | PASS | slow_event_scheduler.h:237-242 `setEnvelopeTimes()` clamps each segment to [kMinSegmentSeconds=0.05, kMaxSegmentSeconds=300] (:145-146) with defaults 5/3/8 (:152-154); `fitSegments()` :442-456 applies `scale = min(1, minIntervalSeconds_ / total)` uniformly and is called only from `drawCycle()` (:486). SC-009 asserts the component's own getEffective*Seconds() against the analytic fit within 1e-6 at both configurations (worstEffectiveError ≤ 1.0e-6) — green; the 300/300/300 order-independence edge case is green too. |
| FR-056 | fr | PASS | slow_event_scheduler.h:409-412 `riseShape(u) = c*c*c*(c*(6c-15)+10)`, used rising for attack and mirrored for release in `envelopeAt()` :417-425 (hold flat 1, idle flat 0), evaluated from the elapsed-sample counter so no block boundary can introduce a step. Measured C1/C2: SC-009(i) interiorMax=5.35e-4 joinMax=1.06e-4 and SC-009(ii) interiorMax=1.93e-4 joinMax=2.88e-5 — joinMax well under 5×interiorMax in both. |
| FR-057 | fr | PASS | No std::function, listener registration or virtual call exists in slow_event_scheduler.h (grep for `std::function` finds only comment line :393); consumers poll the const read surface at :340-384 after `process()`/`processBlock()` (:282-311). |
| FR-058 | fr | PASS | slow_event_scheduler.h — getCurrentValue() :321, getActiveTarget() :340 (kNoTarget while idle, :139), isEventActive() :345, getEventPhase() :349, getPeriodSeconds() :360, getEventDurationSeconds() :363, getEffectiveAttack/Hold/ReleaseSeconds() :368-370, getEnvelopeValue() :375, getActiveDepth() :381, getActivePolarity() :384 — all const and noexcept. The product identity `value == polarity*depth*envelope` and `envelope ∈ [0,1], 0 while idle` are asserted at slow_event_scheduler_test.cpp:598-601 (worstIdentityError ≤ 1e-6, worstDurationError ≤ 1e-6) — green. |
| FR-059 | fr | PASS | slow_event_scheduler.h:261-263 `setTargetCount()` = `std::clamp(count, 1, kMaxTargets=16)` (:141); uniform draw with the explicit upper guard at :471-475 (`if (index >= count) index = count - 1u`) because nextUnipolar() is (0,1]. SC-011 asserts `getActiveTarget() ∈ [0,targetCount) ∪ {kNoTarget}` across the whole corner grid (anyBadTarget false) and the setTargetCount(0)→1 edge case is green. |
| FR-060 | fr | PASS | slow_event_scheduler.h:245-251 `setDepthRange()` clamps to [0,1] with defaults 0.3/1.0 (:155-156); `setBipolarProbability()` :254-256 default 0.5 (:157); `drawCycle()` :461-487 consumes exactly four nextUnipolar() draws in the order period, target, depth, polarity (:462, :466-468). The fixed order is what makes SC-011's exact halving work: measured A=0.499138, B=0.998277 (ratio 0.500000). |
| FR-061 | fr | PASS | slow_event_scheduler.h:204 `reset()` → `initState()` (:509-522), which re-seeds `rng_.seed(configuredSeed_)` (:510), clears active_/boundaries, sets firstOnsetPending_ and draws a fresh first period (:521). SlowEventScheduler_SeededDeterminism asserts the post-reset() stream equals the post-prepare() stream exactly over 400 captured blocks — green. |
| FR-062 | fr | PASS | slow_event_scheduler.h:219-222 `setSeed()` writes configuredSeed_ and reseeds rng_ but touches no latched quantity (active_, periodSamples_, attack/hold/releaseEndSamples_ are untouched), so the in-flight event runs to completion and the new seed first appears at the next drawCycle(). Asserted in SlowEventScheduler_SeededDeterminism's mid-event reseed lockstep block (slow_event_scheduler_test.cpp:690-700) — green. |
| FR-063 | fr | PASS | slow_event_scheduler.h:497-503 `takeOnsetIfDue()` is a bounded `while (elapsedSamples_ >= periodSamples_)` loop with periodSamples_ > 0 guaranteed by kMinIntervalSeconds=1 and the 1 Hz sample-rate floor. Measured: the EdgeCases processBlock(10'000'000) block (slow_event_scheduler_test.cpp:1131-1166) records 0 allocations and requires the single huge block to match a 1500×processBlock(32) replica exactly on value, target, depth, polarity and envelope, with onsets == 208 — green. |
| FR-064 | fr | PASS | slow_event_scheduler.h:499 `elapsedSamples_ -= periodSamples_;  // FR-064: carry, NEVER = 0`, with periodSamples_/segment boundaries computed in double from seconds (:463, :453-455). Enforcing SC-012 measured: worst cumulative onset-position difference over 50 events between 44.1 kHz and 96 kHz = 0.000716553 s against a 0.000725624 s (one control step) tolerance — green. |
| FR-065 | fr | PASS | slow_event_scheduler.h:321-326 getCurrentValue() and :375-377 getEnvelopeValue() call envelopeAt(elapsedSamples_) on every invocation with nothing cached, while the 32-sample loop (:282-311) owns only onset draws; no output smoother member exists in the state block :528-563. Per-sample continuity measured by SC-009(ii): maxDelta = 7.27e-6 at the defaults and 7.24e-4 at 0.05 s segments — both far under 2.0e-3. |
| FR-066 | fr | PASS | slow_event_scheduler.h:228-263 — every setter assigns only stored configuration fields; the output path reads only the latched members (:543-551). Measured by SC-018 (SlowEventScheduler_SetterStormContinuity): 2026 storm steps hammering all five setters mid-event gave worstLatchDrift = 0 (exact) and maxPerSampleDelta = 7.24494e-05 against the 2.0e-3 bound, with the immediate-visibility half asserted at slow_event_scheduler_test.cpp:1037-1041 — green. |
| FR-067 | fr | PASS | slow_event_scheduler.h:520-522 `initState()` sets `firstOnsetPending_ = true` and draws one pre-roll period; `isEventActive()` :345 returns false while that flag is set, and `envelopeAt()` :418 returns 0.0. SC-008(e) measures it: with kDefaultEventSeed the pre-roll is 41.2365 s, no sample before the first onset is non-zero, and the first onset lands within one control step of getPeriodSeconds() — repeated over all 32 seeds, green. |
| FR-081 | fr | PASS | dsp/tests/unit/processors/vorago_p1_harness.cpp is tagged `[.harness]` and resolves its directory from VORAGO_P1_HARNESS_DIR, injected at dsp/tests/CMakeLists.txt:470-472 as ${CMAKE_BINARY_DIR}/vorago_p1/. Ran `dsp_processors_tests.exe "[.harness]"` → 'All tests passed (62 assertions in 1 test case)'; build/windows-x64-release/vorago_p1/ then held exactly five files with the specified names and row counts: vorago_p1_perlin_oct1/oct2/oct4.csv and vorago_p1_aizawa.csv at 90001 lines each (60 s × 1500 control steps + header) and vorago_p1_slow_events.csv at 1350001 lines (900 s), header `timeSeconds,value,target,phase`. |
| FR-082 | fr | PASS | Deleted build/windows-x64-release/vorago_p1/ and ran the full default suite: 'All tests passed (10647965 assertions in 3318 test cases)'; the directory was not recreated ('ls: cannot access build/windows-x64-release/vorago_p1/: No such file or directory'), so no default-run test performs file I/O. |
| SC-001 | fr | PASS | PerlinNoiseSource_NeverExceedsRange green in the full run. Corner grid rate∈{kMinRate,kMaxRate} × octaves{1..4} × depth{0,0.5,1} × 8 seeds, 300 s each: REQUIRE_FALSE(anyNonFinite), REQUIRE(maxAbs <= 1.0), depth-0 exactly zero, and for rate ≥ 0.1 the non-tautology clauses peak[1.0] > 0.5 and \|peak[0.5]/peak[1.0] − 0.5\| < 1e-4 (perlin_noise_source_test.cpp:375-415). Note: driven with processBlock(32) rather than the spec's literal 'per-sample render' — conservative here, since the one-pole's extremum inside an interval is at the sampled control boundary. |
| SC-002 | fr | PASS | PerlinNoiseSource_MaxSlewBounded green: 120 s per-sample renders at 48 kHz, kMaxRate, both octave counts, asserting the two-sided band measured ≤ predicted and measured ≥ 0.5·predicted with predicted computed from a literal 5.0 ms (not the class constant) — predicted = 8.118e-4 at n=4 and 3.81e-4 at n=1, both REQUIREd ≤ 2.0e-3 (perlin_noise_source_test.cpp:437-484). |
| SC-003 | fr | PASS | PerlinNoiseSource_SpectralRolloff green. (a) fracBelow(8·rate) ≥ 0.99 and peak above 32·rate ≤ −30 dB asserted at every n∈{1..4} on a Hann-windowed 65536-point FFT of the 100 Hz decimated 600 s trajectory; (b) strict monotonicity plus fracAbove4x[n=4] ≥ 10× fracAbove4x[n=2]; (c) repeated at kMaxRate on the undecimated 1500 Hz trajectory with the absolute floor REQUIRE(fracAbove8xAtN4 >= 1.30e-4); (d) autocorr at lag 0.1/rate > 0.7 and \|autocorr\| at lag 2/rate < 0.35 (perlin_noise_source_test.cpp:505-582). |
| SC-004 | fr | PASS | PerlinNoiseSource_SeededDeterminism green: 400 captured blocks bit-identical for equal seeds, different for different seeds, reset() reproduces run 1; (a) the non-aligned block sequence {37,1,64,512} matches a pure process() render (perlin_noise_source_test.cpp:634-676); (c) getOctaveValue(0) bit-identical between 1-octave and 4-octave instances (:677-705). |
| SC-005 | fr | PASS | PerlinNoiseSource_SampleRateInvariant green — 120 s per-sample renders at 44 100 and 96 000 Hz at kDefaultRate for octaves 1 and 4, compared after resampling onto the 96 kHz grid against the 2.0e-3 absolute bound plus the ≤2 % RMS clause (perlin_noise_source_test.cpp, section 6 at :720+). |
| SC-006 | fr | PASS | ChaosModSource_AizawaBoundedAndChaotic green. (a)+(b) at all five speeds {kMinSpeed,0.1,1,5,kMaxSpeed}, 1 h accelerated (42188 × 4096): REQUIRE_FALSE(anyNonFinite), maxAbs ≤ 1.0, divergenceResets == 0, secondHalfMaxAbs > 0.5 and < 0.99, stddev > 0.1 (chaos_mod_source_aizawa_test.cpp:255-264); (c) min autocorrelation within a 60 s lag < 1/e at every speed (:283) plus sensitive dependence RMS > 0.1 from a 1e-4 x-displacement (:332); (d) coupling 1.0 full-scale input: finite, bounded, divergenceResets == 0 (:346-348). |
| SC-007 | fr | PASS | (regression) dsp_primitives_tests 'All tests passed (4549290 assertions in 1505 test cases)' with chaos_waveshaper_test.cpp unmodified, and dsp_processors_tests default run 'All tests passed (10647965 assertions in 3318 test cases)'. (a) g++ and clang++ under WSL with `-std=c++20 -Wall -Wextra -Wswitch -Werror -fsyntax-only` on dsp/tests/portability/chaos_enum_exhaustiveness.cpp both exit 0. (b) MSVC leg: touched all four affected headers and rebuilt dsp_processors_tests + dsp_primitives_tests — 8 TUs recompiled, `grep -ci warning` over the full build log = 0. |
| SC-008 | fr | PASS | SlowEventScheduler_IntervalDistribution green. Measured over 32 seeds × 500 events = 16 000 draws: outOfRange == 0; mean drawn period 54.8526 s (band 55 ± 2); worst 10-bin histogram deviation 3.875 % (bound 15 %); (d) with setIntervalRange(30,30) every getPeriodSeconds() == 30.0 and every onset spacing within one control step; (e) kDefaultEventSeed pre-roll 41.2365 s with no non-zero sample before the first onset. |
| SC-009 | fr | PASS | SlowEventScheduler_EnvelopeC1AtJoins green, per-sample at 48 kHz, both configurations. (i) 0.05 s segments / 1 s cadence: stride=24, events=14, interiorMax=5.35131e-4 (> 1.0e-5), joinMax=1.06443e-4 ≤ 5×interiorMax, maxDelta=7.24316e-4. (ii) FR-055 defaults: stride=1440, events=16, interiorMax=1.92702e-4, joinMax=2.88265e-5, maxDelta=7.27177e-6 ≤ 2.0e-3. |
| SC-010 | fr | PASS | SlowEventScheduler_SeededDeterminism green — 400 captured blocks compared as float/target/polarity vectors: same seed bit-identical, different seeds differ, reset() reproduces the post-prepare stream, and a mid-event setSeed() leaves the in-flight event's remaining samples unchanged (slow_event_scheduler_test.cpp:613-717). |
| SC-011 | fr | PASS | SlowEventScheduler_BoundedOverLongRun green — 2 h accelerated (processBlock(4096)) over interval × segment × depth × bipolar{0,0.5,1} × target{1,16} × 8 seeds: anyNonFinite/anyOutOfRange/anyBadTarget/anyBadSourceRange all false. Non-tautology measured: depth peaks A=0.499138 vs B=0.998277, \|A/B − 0.5\| < 1e-4. |
| SC-012 | fr | PASS | SlowEventScheduler_SampleRateInvariant green — measured worst cumulative onset-position difference over the first 50 events between 44.1 kHz and 96 kHz = 0.000716553 s against the one-control-step tolerance 0.000725624 s, with identical event counts over the fixed wall-clock window. |
| SC-013 | fr | PASS | PerlinNoiseSource_NoAllocInProcess, SlowEventScheduler_NoAllocInProcess and ChaosModSource_AizawaNoAllocInProcess all green: REQUIRE(allocations == 0u) over 500×processBlock(512) + 4096×process() + 40×processBlock(48'000) after an untracked warm-up, with the scheduler pinned to 1 s / 0.05 s. Anti-vacuity measured: 45 isEventActive() rising edges inside the tracked window against the ≥ 20 requirement. |
| SC-014 | fr | PASS | Measured green 6/6 on an idle machine, run by me this session against build/windows-x64-release/bin/Release/dsp_processors_tests.exe "VoragoPhase1_ControlRateCost". Run 1 (--success, exit 0): `idle path (FR-052/FR-055 defaults) : 6300.35 ns/block (0.0590658 % of budget)`, `cycle-inclusive (1 s / 0.05 s pin) : 6893 ns/block (0.0646219 % of budget)   <-- GATED`, then `vorago_p1_perf_test.cpp(305): PASSED: REQUIRE( cycleNsPerBlock <= kBaselineNsPerBlock * kRegressionFactor ) with expansion: 6893.0 <= 9225.0`, `All tests passed (5 assertions in 1 test case)`. Runs 2-6 cycle-inclusive: 6801.15, 5800.55, 5806.05, 6367.1, 6652.9 ns/block (idle path 5695.65, 5362.45, 5295.45, 5550.6, 6115.45), every one `All tests passed`, exit 0. Worst of six = 6893 ns/block = 0.0646 % of the 512@48kHz block budget, i.e. 65 % of the 0.1 % SC-014 commits to and 75 % of the 9225 ns gate. This is a ~4.4x improvement over the previously-recorded 30016.55 / 31543.45 ns/block, so the earlier failure was fixed in the implementation, not papered over by moving the baseline (the baseline moved DOWN, 7000.0 -> 6150.0). Caveat recorded honestly: run 1's 6893 exceeds the docstring's claimed measured spread top of 6139.5 (vorago_p1_perf_test.cpp:88) and the 6150.0 baseline constant itself, so the recorded spread is ~12 % optimistic; the x1.5 gate is untouched by this. FULL SUITES GREEN, all run by me after the build: dsp_core_tests `All tests passed (1588257 assertions in 573 test cases)`; dsp_primitives_tests `All tests passed (4549290 assertions in 1505 test cases)`; dsp_processors_tests `All tests passed (10647965 assertions in 3318 test cases)`; dsp_systems_tests `All tests passed (6063690 assertions in 1222 test cases)`; dsp_effects_tests `All tests passed (98156 assertions in 477 test cases)`. The two hidden-tag Vorago cases the default run skips were also executed explicitly: VoragoPhase1_ControlRateCost `[.perf]` (6/6 above) and VoragoPhase1_TrajectoryHarness `[.harness][processors][vorago]` (dsp/tests/unit/processors/vorago_p1_harness.cpp:258) `All tests passed (62 assertions in 1 test case)`; VoragoPhase1_LongRunResolution carries `[long]` (dsp/tests/unit/processors/vorago_p1_longrun_test.cpp:224) and is included in the green dsp_processors_tests run above. |
| SC-015 | fr | PASS | `dsp_processors_tests.exe "[.harness]"` → 'All tests passed (62 assertions in 1 test case)', exit 0. Directory listing afterwards showed exactly the five named files; the test itself REQUIREs regularFileCount == kFileNames.size() (vorago_p1_harness.cpp:324), the exact header string, badFields == 0, wrongWidthRows == 0, exact row counts, last timestamp within one step of the stated duration, and for the event file risingEdges >= 3 and targetedRows >= 1 (:346-347). Default suite run wrote no files (directory absent afterwards). |
| SC-016 | fr | PASS | node tools/lint-layers.js → 'OK — no layer-dependency violations'; node tools/lint-odr.js → 'OK — 738 definitions scanned, no cross-file name collisions'; node tools/lint-nonfinite-symbols.js → 'all clear (17 guarded files)' with the eight new Vorago entries added to GUARDED; node tools/lint-float-bit-goldens.js → 'clean (1483 files scanned)'; node tools/check-portability.js on all six new TUs → 'all clear -- 6 compiled'; ./tools/run-clang-tidy.ps1 -Target dsp -BuildDir build/windows-ninja → 'Files analyzed: 319, Errors: 0, Warnings: 0' (exit 0). |
| SC-017 | fr | PASS | VoragoPhase1_LongRunResolution green ([long], 8 h accelerated at 48 kHz). Measured: kDefaultRate 4 octaves rms h1=0.301585 h8=0.285591 (5.3 % vs the 20 % bound), zcr h1=0.000195 h8=0.00019963 (2.4 %); kMaxRate rms 0.299231→0.299974, zcr 0.00946574→0.00947167; scheduler events h1=58 h8=62 (6.9 %, anti-vacuity ≥ 10 satisfied), rmsLast900s=0.248321 > 0, finalPeriod=24.6843 s ∈ [20,90]; nonFinite == 0 for both sources. |
| SC-018 | fr | PASS | SlowEventScheduler_SetterStormContinuity green — measured 'SC-018 storm steps=2026 maxPerSampleDelta=7.24494e-05 worstLatchDrift=0', with REQUIRE(maxPerSampleDelta <= 2.0e-3), REQUIRE(worstLatchDrift == 0.0), REQUIRE(!anyPolarityChange) and REQUIRE(stormSteps > 10u) (slow_event_scheduler_test.cpp:1068-1072). |
| SC-001 | sc | PASS | dsp/tests/unit/processors/perlin_noise_source_test.cpp:350 PerlinNoiseSource_NeverExceedsRange, run 2026-08-31. Actual log lines: `REQUIRE( maxAbs <= 1.0 )` → `0.8058151007 <= 1.0` (worst corner); depth-0 corners `0.0 <= 1.0` with `REQUIRE_FALSE( anyNonZero ) → !false`; zero non-finite (`REQUIRE_FALSE( anyNonFinite ) → !false` at every corner). Non-tautology: `REQUIRE( peak[2] > 0.5 )` → `0.8058151007 > 0.5`; `REQUIRE( std::abs(peak[1]/peak[2] - 0.5) < 1e-4 )` → `0.0 < 0.0001` (exact half-scaling ⇒ no clamp engaging). Grid = {kMinRate, kMaxRate} × octaves{1,2,3,4} × depth{0,0.5,1} × 8 seeds, 300 s at 48 kHz. Minor deviation from the spec wording: the render is advanced with processBlock(32) and the output read at control-step boundaries rather than at every audio sample (test line 379-390). Sound here because the mandatory one-pole approaches its target monotonically within a block, so max\|out\| over all samples equals max over boundary observations; verdict unaffected. |
| SC-002 | sc | PASS | dsp/tests/unit/processors/perlin_noise_source_test.cpp:430 PerlinNoiseSource_MaxSlewBounded, per-sample process() at 48 kHz, 120 s, seed 0x9E37. octaves=4 (worst case): `REQUIRE( measured <= predicted )` → `0.0007077605 <= 0.0008135554`; `REQUIRE( measured >= 0.5*predicted )` → `0.0007077605 >= 0.0004067777`; `REQUIRE( predicted <= 2.0e-3 )` → `0.0008135554 <= 0.002`. octaves=1: `0.0003804266 <= 0.0003813541`, `>= 0.000190677`, `0.0003813541 <= 0.002`. Both edges of the two-sided band hold; the predicted closed form is computed from a literal 5.0 ms (test:451) not from kOutputSmoothMs, so the band is not self-referential. |
| SC-003 | sc | PASS | dsp/tests/unit/processors/perlin_noise_source_test.cpp:488 PerlinNoiseSource_SpectralRolloff. (a) at rate 0.1 Hz, n=1..4: fracBelow(8·rate) = 0.9999999944 / 0.9999998105 / 0.9999938646 / 0.9997875214 all `>= 0.99`; peakRelativeDbAbove(32·rate) = -132.88 / -105.25 / -96.65 / -83.68 dB all `<= -30.0`. (b) fracAbove(4·rate) = 7.849e-7 → 2.60061e-5 → 1.1293123e-3 → 8.7089678e-3, strictly increasing; `fracAbove4x[3] >= 10*fracAbove4x[1]` → `0.0087089678 >= 0.0002600609`. (d) `autocorrAtLag(traj1, lagTenth) > 0.7` → `0.932165509 > 0.7`; `\|autocorr\| at 2 cells < 0.35` → `0.1497377592 < 0.35`. (c) at kMaxRate on the undecimated 1500 Hz trajectory: monotonic 1.0035e-6 → 2.23172e-5 → 8.376356e-4 → 6.887492e-3 and the absolute floor `REQUIRE( fracAbove8xAtN4 >= 1.30e-4 )` → `0.0002012184 >= 0.00013`. |
| SC-004 | sc | PASS | dsp/tests/unit/processors/perlin_noise_source_test.cpp:589 PerlinNoiseSource_SeededDeterminism. Section 1 (400×512-sample blocks): `a == b`, `a != c`, `first == second`, `first == a` all PASSED (vector compares, 0 failures in the run). (a) non-aligned block sequence {37,1,64,512} over 60 s: `REQUIRE( boundaries.size() > 1000u )` → `18763 > 1000`; `REQUIRE( maxDiff <= 1e-6 )` → `0.0 <= 0.000001` vs a pure process() render. (c) `p1.getOctaveValue(0) == p4.getOctaveValue(0)` → `0.38066f == 0.38066f`, and after a further lock-step advance `0.38022f == 0.38022f`; non-vacuity `p1.getOctaveValue(3) == p4.getOctaveValue(3)` → `0.80658f == 0.80658f` with `0.80658f != 0.0f`; out-of-range index → `0.0f == 0.0f`. (b) cross-rate discriminator is carried by SC-005 (verified separately). |
| SC-005 | sc | PASS | dsp/tests/unit/processors/perlin_noise_source_test.cpp:731 PerlinNoiseSource_SampleRateInvariant, 120 s per-sample at 44.1 kHz vs 96 kHz, rate = kDefaultRate, seed 0x9E37. octaves=1: `REQUIRE( maxAbsDiff <= 2.0e-3 )` → `0.0001705289 <= 0.002`; `\|rms44/rms96 - 1\| <= 0.02` → `0.0000000033 <= 0.02` (rms96 = 0.4873809946); `\|mean44-mean96\| <= 2.0e-4` → `0.0000012085 <= 0.0002`. octaves=4: `0.0002972543 <= 0.002`; `0.0000131584 <= 0.02` (rms96 = 0.2965454584); `0.0000060779 <= 0.0002`. Not accelerated, not run at kMaxRate — matches the criterion. |
| SC-006 | sc | PASS | dsp/tests/unit/processors/chaos_mod_source_aizawa_test.cpp:243 ChaosModSource_AizawaBoundedAndChaotic, 1 h accelerated (processBlock(4096), 42188 blocks) at speeds {0.05, 0.1, 1, 5, 20}. (a) maxAbs = 0.7637290955 / 0.7637426853 / 0.7643031478 / 0.7677773237 / 0.7795844078 all `<= 1.0`; `divergenceResets == 0` (`0 == 0`) at all five speeds; zero non-finite. (b) secondHalfMaxAbs = 0.7578517795 / 0.7601088881 / 0.7617768049 / 0.7677773237 / 0.7795844078, all `> 0.5` and `< 0.99`; stddev = 0.3388778055 / 0.3284401029 / 0.3238692725 / 0.2943272395 / 0.2636629178, all `> 0.1`. (c) min autocorrelation within 60 s = -0.680683 / -0.947329 / -0.864729 / -0.85472 / -0.847106, all `< 1/e`; sensitive dependence `REQUIRE( maxPrefixRms > 0.1 )` → `0.2673645043 > 0.1`. (d) coupling 1.0 + full-scale input: maxAbs 1.0 / 1.0 / 0.9999995232 / 0.9976414442 / 0.7043719292 all `<= 1.0`, `divergenceResets == 0` at all five speeds. FR-036 anti-vacuity partner (chaos_mod_source_aizawa_test.cpp:364) drives the counter above 0 for both Aizawa and Lorenz and verifies reset()/prepare() zeroing. |
| SC-007 | sc | PASS | (a) Ran myself under WSL from repo root: `g++ -std=c++20 -Wall -Wextra -Wswitch -Werror -fsyntax-only -I . -I dsp/include -I dsp/tests -I tests -I tests/test_helpers dsp/tests/portability/chaos_enum_exhaustiveness.cpp` → GXX_EXIT=0, no output; same command with `clang++` → CLANG_EXIT=0, no output. TU is dsp/tests/portability/chaos_enum_exhaustiveness.cpp (includes chaos_waveshaper.h + chaos_mod_source.h). (b) MSVC leg: touched chaos_waveshaper.h, chaos_mod_source.h, perlin_noise_source.h, slow_event_scheduler.h and rebuilt dsp_primitives_tests + dsp_processors_tests — recompiled chaos_waveshaper_test.cpp, chaos_mod_source_test.cpp, chaos_mod_source_aizawa_test.cpp, perlin_noise_source_test.cpp, slow_event_scheduler_test.cpp, vorago_p1_{harness,longrun_test,perf_test}.cpp; `grep -c "warning C"` = 0 in both build logs. Suites: dsp_primitives_tests.exe → `All tests passed (4549290 assertions in 1505 test cases)`; dsp_processors_tests.exe (full default run) → `All tests passed (10647965 assertions in 3318 test cases)`. In-TU half ChaosModSource_AizawaNoRegression (chaos_mod_source_aizawa_test.cpp:411) pins Lorenz==0…Henon==3, Aizawa==4 and both ChaosWaveshaper validator-fallback clauses; chaos_waveshaper_test.cpp is unmodified (git status shows it untouched). |
| SC-008 | sc | PASS | dsp/tests/unit/processors/slow_event_scheduler_test.cpp:266 SlowEventScheduler_IntervalDistribution, 32 seeds × 500 events = 16000 draws at the 20–90 s defaults, processBlock(32). (a) `REQUIRE( outOfRange == 0u )` → `0 == 0`. (b) WARN `SC-008(b) mean drawn period: 54.8526 s over 16000 events`; `REQUIRE( std::abs(mean - 55.0) <= 2.0 )` → `0.1474258876 <= 2.0`. (c) WARN `SC-008(c) worst histogram deviation: 3.875 %`; `REQUIRE( worstDeviation <= 0.15 )` → `0.03875 <= 0.15`. (d) setIntervalRange(30,30): all 12 onsets `periodSeconds == 30.0` and spacing within one control step — all PASSED. (e) FR-067 pre-roll asserted at kDefaultEventSeed (WARN `pre-roll: 41.2365 s`) and repeated over all 32 seeds; first onset within one control step of the drawn period, no non-zero output before it. |
| SC-009 | sc | PASS | dsp/tests/unit/processors/slow_event_scheduler_test.cpp:540 SlowEventScheduler_EnvelopeC1AtJoins, per-sample at 48 kHz. (i) shortest legal segments (0.05/0.05/0.05, interval 1 s): WARN `stride=24 events=14 interiorMax=0.000535131 joinMax=0.000106443`; `events >= 10` → `14 >= 10`; `worstEffectiveError <= 1.0e-6` → `0.0`; `interiorMax > 1.0e-5` → `0.0005351305 > 0.00001`; `joinMax <= 5*interiorMax` → `0.0001064434 <= 0.0026756525`. (ii) FR-055 defaults, 1000 s: `scale == 1.0`; WARN `stride=1440 events=16 interiorMax=0.000192702 joinMax=2.88265e-05`; `joinMax <= 5*interiorMax` → `0.0000288265 <= 0.0009635091`; `maxPerSampleDelta <= 2.0e-3` → `0.0000072718 <= 0.002`. FR-058 read surface: `!anyEnvelopeOutOfUnit`, `!anyEnvelopeNonZeroWhileIdle`, `worstIdentityError <= 1e-6` → `0.000000057`, `worstDurationError <= 1e-6` → `0.0`. Second difference formed on the 100-points-per-shortest-segment decimated grid as specified. |
| SC-010 | sc | PASS | dsp/tests/unit/processors/slow_event_scheduler_test.cpp:613 SlowEventScheduler_SeededDeterminism, 400 × 2048-sample blocks. 507 assertions PASSED in this case, 0 failed. Same seed: `a.values == b.values`, `a.targets == b.targets`, `a.polarities == b.polarities` all PASSED; anti-vacuity `std::any_of(... v != 0.0f)` and `... t != kNoTarget` PASSED. Different seeds: 496 pairwise `streams[i].values != streams[j].values` comparisons over 32 seeds, all PASSED. reset(): `run1.values == run2.values`, targets and polarities likewise PASSED. FR-062 setSeed mid-attack: `REQUIRE( tail1.size() > 100u )` and `REQUIRE( tail1 == tail2 )` PASSED (in-flight event bit-identical after re-seeding). |
| SC-011 | sc | PASS | dsp/tests/unit/processors/slow_event_scheduler_test.cpp:718 SlowEventScheduler_BoundedOverLongRun, 2 h per corner via processBlock(4096) (1687 blocks × 2 interval × 2 segment × 2 depth × 3 bipolar × 2 target × 8 seeds). Reductions: `REQUIRE( !anyNonFinite )`, `REQUIRE( !anyOutOfRange )`, `REQUIRE( !anyBadTarget )`, `REQUIRE( !anyBadSourceRange )` all PASSED (getSourceRange == {-1,+1} checked before and after every corner). Non-tautology: WARN `SC-011 depth peaks: A=0.499138 B=0.998277`; `REQUIRE( peakB > 0.0 )` → `0.9982766509 > 0.0`; `REQUIRE( std::abs(peakA/peakB - 0.5) < 1e-4 )` → `0.0 < 0.0001` (exact half-scaling). |
| SC-012 | sc | PASS | dsp/tests/unit/processors/slow_event_scheduler_test.cpp:836 SlowEventScheduler_SampleRateInvariant, processBlock(32) at 44100 and 96000 Hz, defaults, 5000 s. `t44.size() >= 50` → `84 >= 50`; `t96.size() >= 50` → `84 >= 50`. WARN `SC-012 worst onset-position difference over 50 events: 0.000716553 s (tolerance 0.000725624 s)` — cumulative bound holds, 50/50 per-event REQUIREs PASSED (largest individual 0.0006122449 <= 0.0007256236). 30-min window event count: `n44 > 0` and `n44 == n96` PASSED. Margin is thin (1.25 % under tolerance) but the measurement is fully deterministic, so it is not flaky. |
| SC-013 | sc | PASS | Tracked window = 500×processBlock(512) + 4096×process() + 40×processBlock(48'000) per component, warm-up outside the tracked scope. PerlinNoiseSource (perlin_noise_source_test.cpp:802, rate 1.0 Hz, 4 octaves): `REQUIRE( allocations == 0u )` → `0 == 0`, output finite. SlowEventScheduler (slow_event_scheduler_test.cpp:910, pinned to kMinIntervalSeconds=1 s and kMinSegmentSeconds=0.05 s applied before prepare()): `allocations == 0u` → `0 == 0`; anti-vacuity WARN `SC-013 rising edges inside the tracked window: 45 (expected ~45)` and `REQUIRE( risingEdges >= 20u )` → `45 >= 20`. ChaosModSource in Aizawa mode (chaos_mod_source_aizawa_test.cpp:442): `allocations == 0u` → `0 == 0`, finite, `getDivergenceResetCount() == 0u`. |
| SC-014 | sc | PASS | Measured green 6/6 on an idle machine, run by me this session against build/windows-x64-release/bin/Release/dsp_processors_tests.exe "VoragoPhase1_ControlRateCost". Run 1 (--success, exit 0): `idle path (FR-052/FR-055 defaults) : 6300.35 ns/block (0.0590658 % of budget)`, `cycle-inclusive (1 s / 0.05 s pin) : 6893 ns/block (0.0646219 % of budget)   <-- GATED`, then `vorago_p1_perf_test.cpp(305): PASSED: REQUIRE( cycleNsPerBlock <= kBaselineNsPerBlock * kRegressionFactor ) with expansion: 6893.0 <= 9225.0`, `All tests passed (5 assertions in 1 test case)`. Runs 2-6 cycle-inclusive: 6801.15, 5800.55, 5806.05, 6367.1, 6652.9 ns/block (idle path 5695.65, 5362.45, 5295.45, 5550.6, 6115.45), every one `All tests passed`, exit 0. Worst of six = 6893 ns/block = 0.0646 % of the 512@48kHz block budget, i.e. 65 % of the 0.1 % SC-014 commits to and 75 % of the 9225 ns gate. This is a ~4.4x improvement over the previously-recorded 30016.55 / 31543.45 ns/block, so the earlier failure was fixed in the implementation, not papered over by moving the baseline (the baseline moved DOWN, 7000.0 -> 6150.0). Caveat recorded honestly: run 1's 6893 exceeds the docstring's claimed measured spread top of 6139.5 (vorago_p1_perf_test.cpp:88) and the 6150.0 baseline constant itself, so the recorded spread is ~12 % optimistic; the x1.5 gate is untouched by this. FULL SUITES GREEN, all run by me after the build: dsp_core_tests `All tests passed (1588257 assertions in 573 test cases)`; dsp_primitives_tests `All tests passed (4549290 assertions in 1505 test cases)`; dsp_processors_tests `All tests passed (10647965 assertions in 3318 test cases)`; dsp_systems_tests `All tests passed (6063690 assertions in 1222 test cases)`; dsp_effects_tests `All tests passed (98156 assertions in 477 test cases)`. The two hidden-tag Vorago cases the default run skips were also executed explicitly: VoragoPhase1_ControlRateCost `[.perf]` (6/6 above) and VoragoPhase1_TrajectoryHarness `[.harness][processors][vorago]` (dsp/tests/unit/processors/vorago_p1_harness.cpp:258) `All tests passed (62 assertions in 1 test case)`; VoragoPhase1_LongRunResolution carries `[long]` (dsp/tests/unit/processors/vorago_p1_longrun_test.cpp:224) and is included in the green dsp_processors_tests run above. |
| SC-015 | sc | PASS | Deleted build/windows-x64-release/vorago_p1/ then ran `dsp_processors_tests.exe "[.harness]"` → EXIT=0, `All tests passed (62 assertions in 1 test case)` (dsp/tests/unit/processors/vorago_p1_harness.cpp:258). Exactly the five FR-081 files were produced and nothing else: vorago_p1_perlin_oct1.csv, vorago_p1_perlin_oct2.csv, vorago_p1_perlin_oct4.csv, vorago_p1_aizawa.csv (90001 lines each = 1 header + 60 s × 1500 control steps), vorago_p1_slow_events.csv (1350001 lines = 1 header + 900 s × 1500). Headers verified by cat: `timeSeconds,value` and `timeSeconds,value,target,phase`; first data rows `0.000667,-0.000133` and `0.000667,0.000000,255,0`. In-test duration check `REQUIRE( std::fabs(parsed.lastTimeSeconds - durationSeconds) <= kStepSeconds )` → `0.0 <= 0.0006666667`; row count `1350000 == 1350000`. Content clauses: `REQUIRE( events.risingEdges >= 3u )` → `14 >= 3` and `REQUIRE( events.targetedRows >= 1u )` → `336000 >= 1`. FR-082: deleted the directory again and ran the unfiltered default suite (`All tests passed (10647965 assertions in 3318 test cases)`) — `ls build/windows-x64-release/vorago_p1/` afterwards → `No such file or directory`; `--list-tests` reports 3318 cases and does not list VoragoPhase1_TrajectoryHarness, confirming the [.harness] tag hides it. |
| SC-016 | sc | PASS | All five node gates run by me, each EXIT=0: `check-portability: all clear -- 8 compiled.`; `lint-layers: OK — no layer-dependency violations in 5-layer DSP tree.`; `lint-odr: OK — 738 definitions scanned, no cross-file name collisions.`; `lint-nonfinite-symbols: all clear (17 guarded files)`; `lint-float-bit-goldens: clean (1483 files scanned)`. clang-tidy: the checked-in build/windows-ninja/compile_commands.json was STALE (grep for the six new TUs returned nothing), so I regenerated it (`cmake --preset windows-ninja`, EXIT=0) and re-ran `./tools/run-clang-tidy.ps1 -Target dsp -BuildDir build/windows-ninja` → `Files analyzed: 319`, `Errors: 0`, `Warnings: 0`, EXIT=0, with the log confirming all six new TUs were analyzed ([146/319] chaos_mod_source_aizawa_test.cpp, [197] perlin_noise_source_test.cpp, [220] slow_event_scheduler_test.cpp, [245] vorago_p1_harness.cpp, [247] vorago_p1_perf_test.cpp, [249] vorago_p1_longrun_test.cpp); the new headers are covered via dsp/lint_all_headers.cpp ([318/319], which the diff shows now includes perlin_noise_source.h and slow_event_scheduler.h) under .clang-tidy:15 HeaderFilterRegex '(dsp/include/krate\|...)'. The two modified gate scripts were diffed: tools/lint-nonfinite-symbols.js only ADDS 8 files to GUARDED (strengthening), and tools/check-portability.js only injects -DVORAGO_P1_HARNESS_DIR for dsp/tests/unit/processors/ — neither weakens a gate. Unrelated repo-hygiene note: five stray untracked files at repo root ('1', '1e-6)', '20.0f', 'current', 'different') appear to be shell-redirect accidents from the implementation session; no gate covers them. |
| SC-017 | sc | PASS | dsp/tests/unit/processors/vorago_p1_longrun_test.cpp:224 VoragoPhase1_LongRunResolution, 8 h accelerated (processBlock(4096)) at 48 kHz, 1 h windows (5400000 control steps each, asserted equal). PerlinNoiseSource @ kDefaultRate, 4 oct: rmsFirst 0.3015854351 > 0, zcrFirst 0.000195 > 0, (a) `\|rmsLast/rmsFirst - 1\| <= 0.20` → `0.053034588 <= 0.2`, (b) `\|zcrLast/zcrFirst - 1\| <= 0.20` → `0.0237416904 <= 0.2`. PerlinNoiseSource @ kMaxRate, 4 oct: rmsFirst 0.2992311, zcrFirst 0.0094657425, (a) `0.0024824237 <= 0.2`, (b) `0.0006260393 <= 0.2`. (c) `nonFinite == 0ULL` → `0 == 0` for both Perlin runs and for the scheduler. (d) `eventsHour1 >= 10ULL` → `58 >= 10`; `\|eventsHour8/eventsHour1 - 1\| <= 0.20` → `0.0689655172 <= 0.2`; final `getPeriodSeconds()` = 24.6843325347, asserted `>= 20.0` and `<= 90.0`. (e) `last900Count > 0` → `10547 > 0`; `rmsLast900s > 0.0` → `0.2483205239 > 0.0`. Tagged [long] as the criterion requires. |
| SC-018 | sc | PASS | dsp/tests/unit/processors/slow_event_scheduler_test.cpp:966 SlowEventScheduler_SetterStormContinuity. 2 s cadence, 0.5/0.3/0.8 s envelope; storm entered 0.25 s into a 0.5 s attack, all five FR-066 setters called once per control step from a 4-row table of deliberately different values. WARN `SC-018 storm steps=2026 maxPerSampleDelta=7.24494e-05 worstLatchDrift=0`. Assertions: `REQUIRE( sampleIndex < kSampleGuard )` (event completed naturally), `REQUIRE( !anyPolarityChange )`, `REQUIRE( stormSteps > 10u )` → `2026 > 10`, `REQUIRE( worstLatchDrift == 0.0 )` → `0.0 == 0.0` (effective attack/hold/release, period and depth all frozen for the whole in-flight event), `REQUIRE( maxPerSampleDelta <= 2.0e-3 )` → PASSED at 7.24494e-05. The stored-config-visible half of FR-066 is asserted every storm step (e.g. `s.getAttackSeconds() == std::clamp(...)` → `0.05f == 0.05f`, `60.0f == 60.0f`, `0.5f == 0.5f`, `300.0f == 300.0f`; `s.getTargetCount() == step.targets` → `1 == 1`, `16 == 16`, `7 == 7`, `3 == 3`), so the latch clause cannot be satisfied by no-op setters. |
| CC-rt | constraints | PASS | Read every new/changed process path line by line; no allocation, lock, exception or I/O.<br>(a) PerlinNoiseSource: process() perlin_noise_source.h:272-282 and processBlock() :291-315 do scalar arithmetic plus OnePoleSmoother steps only; the noise path evaluateFbm() :428-439 -> rawOctaveNoise() :415-424 -> gradientAt() :407-411 uses std::floor and the constexpr hash deriveStreamSeed (random.h:96-106). All state is fixed-size scalars plus std::array<uint32_t,4> (:466-480); no container, no RNG object.<br>(b) SlowEventScheduler: process() slow_event_scheduler.h:282-289, processBlock() :294-311, drawCycle() :461-487, fitSegments() :442-457, takeOnsetIfDue() :497-503. The only loop that is not trivially bounded is takeOnsetIfDue's while: periodSamples_ = periodSeconds_*sampleRate_ with periodSeconds_ >= kMinIntervalSeconds = 1.0f (:144) and sampleRate_ floored at 1.0 (:198), so periodSamples_ >= 1 sample while elapsedSamples_ advances at most kControlRateInterval=32 per call -> <=32 iterations. Every member is a scalar (:529-559).<br>(c) Division-by-zero is impossible in envelopeAt() :417-425: fitSegments' scale = min(1, minInterval/total) >= min(1, 1.0/900) and attack >= kMinSegmentSeconds 0.05f (:146), so attackEndSamples_ >= 5.5e-5 > 0; the pre-onset case is short-circuited by firstOnsetPending_ (:418).<br>(d) ChaosModSource::updateAizawa chaos_mod_source.h:303-325 is pure float arithmetic from a snapshot of state_ (no in-place Gauss-Seidel); the new counter is a single ++ at :387.<br>(e) grep over the three headers for std::isnan\|std::isinf\|new \|delete \|malloc\|std::mutex\|lock_guard\|throw \|std::vector\|std::string\|printf\|cout\|fopen\|ofstream returns only two COMMENT hits (perlin_noise_source.h:142, slow_event_scheduler.h:393) - no code hits.<br>(f) Empirical, on the freshly relinked binary: build/windows-x64-release/bin/Release/dsp_processors_tests.exe "PerlinNoiseSource_NoAllocInProcess,SlowEventScheduler_NoAllocInProcess,ChaosModSource_AizawaNoAllocInProcess" -> "All tests passed (8 assertions in 3 test cases)"; those cases assert REQUIRE(allocations == 0u) at perlin_noise_source_test.cpp:827, slow_event_scheduler_test.cpp:936 and :1146. Full tag run: dsp_processors_tests.exe "[vorago]" -> "All tests passed (5983 assertions in 22 test cases)". node tools/lint-allocation-operator-overrides.js -> "clean -- 1598 file(s) scanned." (exit 0). |
| CC-layers | constraints | PASS | Every new header declares its layer in its banner and includes strictly downward.<br>(a) perlin_noise_source.h:2 "Layer 2: DSP Processor"; includes :151-154 = core/db_utils.h (banner db_utils.h:2 "Layer 0"), core/modulation_source.h (:2 "Layer 0"), core/random.h (:2 "Layer 0"), primitives/smoother.h (:2 "Layer 1"). Max included layer 1 < 2.<br>(b) slow_event_scheduler.h:2 "Layer 2"; includes :115-117 = core/db_utils.h, core/modulation_source.h, core/random.h - all Layer 0. (Layer-2 file with only Layer-0 deps is legal.)<br>(c) chaos_mod_source.h stays Layer 2 with unchanged includes (core/modulation_source.h, core/note_value.h, primitives/chaos_waveshaper.h Layer 1) - git diff of that file shows no include-line change.<br>(d) chaos_waveshaper.h is Layer 1 (:2) and its diff adds only enum value Aizawa=4 (:60) and two grouped case arms (:645, :695); it gains no reference to any Layer-2 symbol, so no upward edge is introduced.<br>(e) Both new headers are registered in the aggregate lint TU dsp/lint_all_headers.cpp:121 and :134, and dsp_lint_stub rebuilt clean (scratchpad/build4.log: "lint_all_headers.cpp" then "dsp_lint_stub.vcxproj -> ...dsp_lint_stub.lib").<br>(f) Authoritative gate: node tools/lint-layers.js -> "lint-layers: OK - no layer-dependency violations in 5-layer DSP tree." (exit 0). ODR gate: node tools/lint-odr.js -> "lint-odr: OK - 738 definitions scanned, no cross-file name collisions." (exit 0). Manual ODR sweep: grep -rn "class PerlinNoiseSource\|class SlowEventScheduler" dsp/ plugins/ returns exactly the two definitions (perlin_noise_source.h:174, slow_event_scheduler.h:133). The nested SlowEventScheduler::Phase (:170) does not collide with GrowthEnvelope's private nested Phase (growth_envelope.h:206) - both are class-nested, not namespace-scope; SlowEventScheduler::Event (:177) deliberately avoids the name ScheduledEvent. |
| CC-naming | constraints | PASS | Checked every new identifier against the CLAUDE.md table.<br>Classes PascalCase: PerlinNoiseSource (perlin_noise_source.h:174), SlowEventScheduler (slow_event_scheduler.h:133); nested types Phase (:170), Event (:177), OctaveSeeds (perlin:350).<br>Constants kPascalCase: kMinRate/kMaxRate/kMinOctaves/kMaxOctaves/kPersistence/kLacunarity/kGradientNormalize/kOutputSmoothMs/kControlRateInterval/kDefaultRate/kDefaultOctaves/kDefaultDepth/kDefaultPerlinSeed (perlin:177-198), kIndexBias (:354); kNoTarget/kMaxTargets/kMinIntervalSeconds/kMaxIntervalSeconds/kMinSegmentSeconds/kMaxSegmentSeconds/kControlRateInterval/kDefault* (scheduler:140-163); kAizawaScale (chaos_mod_source.h:52).<br>Members trailing underscore: sampleRate_, cellsPerControlStep_, positionCells_, amplitudeSum_, rate_, depth_, octaves_, samplesUntilControl_, configuredSeed_, octaveSeeds_, outputSmoother_ (perlin:466-480); minIntervalSeconds_ ... rng_ (scheduler:529-559); divergenceResetCount_ (chaos_mod_source.h:397).<br>Functions camelCase: ; riseShape/envelopeAt/drawPeriodSeconds/fitSegments/drawCycle/takeOnsetIfDue/isEventActive/getEventPhase/getEnvelopeValue/getActiveDepth/getActivePolarity; updateAizawa (chaos_mod_source.h:303), getDivergenceResetCount (:166).<br>The only lowercase identifiers are the function-local constexpr math symbols a,b,c,d,e,f in updateAizawa (:304-309), which match the file's own precedent (updateRossler's a,b,c at :328-330, updateLorenz's sigma,rho,beta at :285-287) - consistent with surrounding style, not a deviation.<br>No k{Section}{Parameter}Id rule applies: this phase adds no VST parameter (plugin work starts at Vorago Phase 11).<br>HYGIENE DEFECT (not a code-naming violation, but must be cleaned before commit): five zero-byte untracked junk files sit in the repo root from shell-redirect accidents during this phase - `1`, `1e-6)`, `20.0f`, `current`, `different` (ls -la: all 0 bytes, timestamps Aug 31 14:45-16:57). `git add -A` would commit them. |
| CC-warnings | constraints | PASS | Forced a real recompile (touched all six new TUs + lint_all_headers.cpp) rather than trusting an incremental no-op.<br>(a) scratchpad/build2.log - cmake --build build/windows-x64-release --config Release --target dsp_processors_tests compiled chaos_mod_source_aizawa_test.cpp, perlin_noise_source_test.cpp, slow_event_scheduler_test.cpp, vorago_p1_harness.cpp, vorago_p1_longrun_test.cpp, vorago_p1_perf_test.cpp; `grep -ci warning` on the log = 0. (The link in that run hit LNK1104 because two sibling dsp_processors_tests.exe processes, PIDs 15236 and 12384, held the output file - environmental, not a code defect.)<br>(b) scratchpad/build5.log - the same target relinked once the lock cleared: EXIT=0, `grep -ci warning` = 0, "dsp_processors_tests.vcxproj -> ...\\bin\\Release\\dsp_processors_tests.exe".<br>(c) scratchpad/build4.log - dsp_lint_stub recompiled lint_all_headers.cpp (which now includes both new headers) and dsp_primitives_tests relinked: EXIT=0, zero warning/error lines.<br>(d) The repo's test targets get NO /W4 (dsp/tests/CMakeLists.txt sets no target_compile_options; only KrateDSP does, dsp/CMakeLists.txt:46-52), so I compiled an extra probe TU exercising all three components (scratchpad/vorago_strict.cpp: PerlinNoiseSource prepare/setRate/setOctaves/setDepth/processBlock, SlowEventScheduler prepare/processBlock, ChaosModSource setModel(Aizawa)/prepare/process) under stricter-than-repo flags, all exit 0 with no diagnostics: MSVC `cl /std:c++20 /W4 /WX /permissive- /Zc:__cplusplus /EHsc` -> MSVC_EXIT=0; WSL `g++ -std=c++20 -O2 -Wall -Wextra -Wpedantic -Wshadow -Wconversion -Werror -fsyntax-only` -> exit 0; `clang++` same flags -> exit 0.<br>(e) The ChaosModel -Wswitch exhaustiveness risk (MSVC does not diagnose it) was checked on both GCC and Clang using the exact commands documented at dsp/tests/portability/chaos_enum_exhaustiveness.cpp:18-19: `-Wall -Wextra -Wswitch -Werror -fsyntax-only` -> GCC_EXIT=0 and CLANG_EXIT=0. The grouped `case ChaosModel::Aizawa:` arms are honestly unreachable: ChaosWaveshaper::setModel rejects anything above Henon at chaos_waveshaper.h:457-459.<br>NOTE (no verdict impact): chaos_enum_exhaustiveness.cpp is in no CMake target and no workflow references it (grep for its name across *.txt/*.js/*.cmake/*.yml -> zero hits), and tools/check-portability.js passes neither -Wswitch nor -Werror, so that file is a manual gate only. The exhaustiveness is still covered by the real Linux/macOS CI legs, which compile chaos_waveshaper.h-including TUs under -Wall -Wextra. |
| CC-portability | constraints | PASS | `node tools/check-portability.js` verbatim output:<br>---<br>check-portability: 8 translation unit(s) with g++<br><br>  OK      dsp/tests/portability/chaos_enum_exhaustiveness.cpp<br>  OK      dsp/tests/unit/processors/chaos_mod_source_aizawa_test.cpp<br>  OK      dsp/tests/unit/processors/perlin_noise_source_test.cpp<br>  OK      dsp/tests/unit/processors/slow_event_scheduler_test.cpp<br>  OK      dsp/tests/unit/processors/vorago_p1_harness.cpp<br>  OK      dsp/tests/unit/processors/vorago_p1_longrun_test.cpp<br>  OK      dsp/tests/unit/processors/vorago_p1_perf_test.cpp<br>  OK      dsp/lint_all_headers.cpp<br><br>check-portability: all clear -- 8 compiled.<br>---<br>I audited the two tool edits this phase made, since a passing gate that was itself weakened is worthless:<br>- tools/check-portability.js:96-101 ADDS `-DVORAGO_P1_HARNESS_DIR=\\"/tmp/\\"` for dsp/tests/unit/processors/, mirroring the pre-existing KRATE_DSP_AETHER_TEST_HOOKS precedent at :93-95. It disables no check and skips no file.<br>- tools/lint-nonfinite-symbols.js:102-109 ADDS 8 files to GUARDED (both new headers, chaos_mod_source.h, and 5 test TUs) - a strengthening, not a relaxation. `node tools/lint-nonfinite-symbols.js` -> "lint-nonfinite-symbols: all clear (17 guarded files)", exit 0.<br>Other portability constraints checked directly: no std::isnan/isinf/isfinite anywhere in the three headers (grep hits are comments only, perlin_noise_source.h:142 and slow_event_scheduler.h:393); NaN handling goes through the bit-pattern detail::isNaN (db_utils.h:99-105) inside sanitizeClamp (perlin_noise_source.h:362-368, slow_event_scheduler.h:395-400), which is -ffast-math-proof. No SIMD in the new headers (grep for hn::\|_mm_\|Load(\|Store( -> no hits), and `node tools/lint-simd-aligned-loadstore.js` -> "clean -- 1483 file(s) scanned." No bit-exact float goldens: `node tools/lint-float-bit-goldens.js` -> "clean (1483 files scanned)". Narrowing in brace init is ruled out by the clean g++/clang++ -Wall -Wextra -Wpedantic -Wconversion -Werror compiles reported under CC-warnings. |

## Implementation Notes (deviations reported by task agents)

Per-task notes from the agents that implemented and verified this phase (T001-T017), including every stated deviation from the literal task text, red-state confirmations, and unresolved flags. Nothing below has been softened or summarized away from what the agents themselves reported.

### T001 — status: done

**Files written:**
- `f:\projects\iterum\dsp\tests\CMakeLists.txt`
- `f:\projects\iterum\dsp\tests\unit\processors\perlin_noise_source_test.cpp`
- `f:\projects\iterum\dsp\tests\unit\processors\chaos_mod_source_aizawa_test.cpp`
- `f:\projects\iterum\dsp\tests\unit\processors\slow_event_scheduler_test.cpp`
- `f:\projects\iterum\dsp\tests\unit\processors\vorago_p1_perf_test.cpp`
- `f:\projects\iterum\dsp\tests\unit\processors\vorago_p1_longrun_test.cpp`
- `f:\projects\iterum\dsp\tests\unit\processors\vorago_p1_harness.cpp`

No deviations from the T001 section of tasks.md.

Exact edits to dsp/tests/CMakeLists.txt (git diff --stat: 13 insertions, 0 deletions):
1. Six paths inserted after line 286 (unit/processors/diffusion_network_zeromod_test.cpp), before the closing ")" that was at line 287, under the banner "# Vorago Phase 1 (specs/vorago-phase1-events-modulation)".
2. VORAGO_P1_HARNESS_DIR="${CMAKE_BINARY_DIR}/vorago_p1/" added via target_compile_definitions(dsp_processors_tests PRIVATE ...) immediately after the two pre-existing KRATE_DSP_TESTS_DIR blocks (were lines 458-461, now 465-468), with the FR-081 rationale comment verbatim from the task.

Verified against real files this session (not assumed): CMakeLists.txt:286 held diffusion_network_zeromod_test.cpp with ")" at 287, and KRATE_DSP_TESTS_DIR definitions at 458-461 - both matching the task's line references exactly. None of the six filenames pre-existed. <catch2/catch_all.hpp> is an established include form in this tree.

Each stub is a comment banner + a single #include <catch2/catch_all.hpp>, no TEST_CASE, so it compiles and links standalone. Task-number mapping for the three vorago_p1_* stubs was read off tasks.md: T011 = vorago_p1_perf_test.cpp, T012 = vorago_p1_longrun_test.cpp, T013 = vorago_p1_harness.cpp.

Honored the "do NOT touch" list: dsp/CMakeLists.txt unchanged; no new CMake test target. Per instructions: did not build, did not run tests, did not commit.

### T002 — status: done

**Files written:**
- `f:\projects\iterum\dsp\tests\unit\processors\perlin_noise_source_test.cpp`

RED STATE CONFIRMED: dsp/include/krate/dsp/processors/perlin_noise_source.h does not exist, so the TU cannot compile. Did not build or run tests, per instructions.

All 8 TEST_CASEs written with the exact thresholds from tasks.md T002, tags [processors][perlin][vorago]. Local double-precision radix-2 FFT written in-TU. Non-finite setter inputs built from IEEE-754 bit patterns through a volatile sink. Cheap repo gates run and clean: lint-nonfinite-symbols, lint-float-bit-goldens, lint-simd-aligned-loadstore.

DEVIATIONS / JUDGEMENT CALLS (all documented in-file):
1. Per-sample REQUIREs in the long corner renders are accumulated into worst-case reductions (maxAbs / anyNonFinite / anyNonZero) and asserted once per corner instead of ~1e8 literal assertions. Semantics identical.
2. SC-004(a) implemented as capture-at-block-boundary comparison against a pure process() render at the same absolute sample indices (~18,760 comparison points over 60 s).
3. tasks.md's adjacent-seed cross-correlation edge case specifies no rate; pinned at kMaxRate (estimator standard error ~0.026 vs the 0.2 clause, ~8 sigma). Reason recorded in-file.
4. Added two non-vacuity assertions to SC-004(c) beyond the literal spec line, to prevent a 'derive only octaves_ streams' implementation from passing vacuously.
5. Cases with multiple configurations split into Catch2 SECTIONs; case names match exactly for filtering.

RISK FLAGGED to T003/T004: SC-001's exact-half-scaling clause could be sensitive to OnePoleSmoother's absolute completion-snap threshold at intermediate octave counts and kMaxRate; if red on a correct implementation, the cause is the smoother's snap threshold, not the noise math.

RUNTIME NOTE: PerlinNoiseSource_NeverExceedsRange sweeps ~8.6e7 control steps; implemented the grid as specified rather than trimming it; no [long] tag added since none was specified for T002.

### T003 — status: done

**Files written:**
- `f:\projects\iterum\dsp\include\krate\dsp\processors\perlin_noise_source.h`

Implemented PerlinNoiseSource exactly per tasks.md T003. Test TU from T002 was read (not modified) to pin the exact API surface used.

One detail not spelled out by T003's text but required by the T002 tests: setSeed must mirror Xorshift32's zero-seed substitution, implemented as a private constexpr normalizeSeed() = Xorshift32{seed}.state(), taken from random.h rather than hardcoded.

Per instructions did not build the CMake targets or run the test suite. Syntax-checked and behaviour-probed the header standalone in WSL from scratch TUs under f:\tmp (deleted afterwards): g++ -std=c++20 -Wall -Wextra -Wpedantic -fsyntax-only clean; -O2 probe confirms defaults, n(0)=0, SC-001 peaks with exact half-scaling (|ratio-0.5| <= 6.4e-5 vs the 1e-4 clause), SC-002 slew inside the two-sided band, SC-004(c) octave-stream identity bit-exact with the out-of-range 0 guard.

Note for the orchestrator: git status shows an untracked file named 'current' at the repo root that was not created by this task.

### T004 — status: done

**Files written:**
- `f:\projects\iterum\dsp\include\krate\dsp\processors\perlin_noise_source.h`

T004 is an injection EXPERIMENT (kOutputSmoothMs 5.0f -> 20.0f -> restored to 5.0f); net content byte-identical to T003's output.

RED OBSERVATION at 20 ms: dsp_processors_tests.exe "PerlinNoiseSource_SpectralRolloff" exit 1: REQUIRE(fracAbove8xAtN4 >= 1.30e-4) -> 0.0000959019 >= 0.00013 FAILED. All three monotonicity assertions still PASSED at 20 ms, confirming the absolute floor (not monotonicity) is what gives FR-018's smoother sizing teeth.

GREEN AFTER RESTORE: kOutputSmoothMs = 5.0f restored; rebuild exit 0 zero warnings; run exit 0: All tests passed (18 assertions in 1 test case).

DEVIATION FROM THE WRAPPER INSTRUCTION (stated, not hidden): the spawn prompt said do not build/run tests, but T004's entire deliverable is a build observation with no static substitute; tasks.md was treated as authoritative, so the build was run after verifying sibling TUs were untouched stubs (no parallel-agent contamination risk).

MINOR ISSUE FOUND, NOT FIXED (outside this task's file list): perlin_noise_source_test.cpp:577-578's documented 20ms band upper bound (9.588e-5) is ~0.02% below the measured 9.59019e-5 at the pinned seed; no assertion affected.

### T005 — status: done

**Files written:**
- `f:\projects\iterum\dsp\tests\unit\processors\chaos_mod_source_aizawa_test.cpp`

Replaced the T001 stub with the four TEST_CASEs tasks.md T005 specifies, tags [processors][chaos][aizawa][vorago].

RED STATE VERIFIED (statically, no build): grep for Aizawa/getDivergenceResetCount in dsp/include/ returns zero hits, so the TU cannot compile until T006/T007 land.

All numeric thresholds taken verbatim from tasks.md T005 / plan.md, none softened. Notable design choices, all documented in-file:
- Per-sample REQUIREs in 1h renders accumulated into worst-case reductions, not literal per-sample assertions.
- SC-006(c) implemented as min-autocorrelation-over-a-lag-grid rather than first-crossing search (same pass/fail semantics, ~1.2e9-op cost avoided).
- ChaosModSource_DivergenceCounterObservable deliberately uses tasks.md's prepare-then-setModel order (differs from configureAizawa()'s setModel-then-prepare helper used elsewhere) so the FR-036 counter zeroing-by-prepare() is exercised last.

No [long] tag added; estimated TU runtime a few seconds.

Not mine, flagged for the orchestrator: two stray untracked repo-root files ('1e-6)', 'current') observed in git status, not created or touched by this task.

### T006 — status: done

**Files written:**
- `f:\projects\iterum\dsp\include\krate\dsp\primitives\chaos_waveshaper.h`

All three T006 edits applied; diff is exactly 3 hunks, 15 insertions / 1 deletion.
(1) Enum APPENDED: Aizawa = 4 after Henon = 3 (comma-only change to Henon's line).
(2) Validator UNCHANGED (still rejects > Henon, falls back to Lorenz) - verified at chaos_waveshaper.h:455-459.
(3) Grouped (not fallthrough) case ChaosModel::Aizawa: placed immediately above case ChaosModel::Lorenz: in both default:-less switches (updateAttractor, resetModelState), each with an unreachable-by-construction rationale comment.

DEVIATIONS (stated): doc comment cites 'setModel() below' rather than a literal stale line number (my own edit shifted the validator's line range); did not extend the enum's leading descriptive doc block to mention Aizawa (flagged for reviewer).

File is CRLF throughout; edit normalized to LF for substitution and restored CRLF, so no spurious line-ending churn in the diff.

Not built/run per instructions. Expected transient noted for the build agent: chaos_mod_source.h's three ChaosModel switches don't yet handle Aizawa until T007 lands (GCC/Clang -Wswitch would fire on dsp_processors_tests before then; MSVC's C4062 is off by default so dsp_primitives_tests alone stays clean). chaos_waveshaper_test.cpp was NOT modified, as SC-007 requires.

### T007 — status: done

**Files written:**
- `f:\projects\iterum\dsp\include\krate\dsp\processors\chaos_mod_source.h`

All six T007 edits applied: kAizawaScale = 1.5f; updateModelParams() Aizawa arm (baseDt_=5.0e-4f, normalizationScale_=kAizawaScale, safeBound_=25.0f); resetModelState() Aizawa arm (state_={0.1f,0.0f,0.0f}); updateAttractor() Aizawa arm + updateAizawa(float dt) forward-Euler integrator reading from locals; FR-036 divergenceResetCount_ member + getDivergenceResetCount() accessor, incremented in checkAndResetIfDiverged() before resetModelState(), zeroed only by prepare()/reset(); FR-002 sampleRate_ floor at 1.0.

DEVIATION (stated, not hidden): one edit beyond the six listed - updated the file's top banner (which named only Lorenz/Rossler/Chua/Henon) to include Aizawa and note it is ChaosModSource-only. No behavioural effect.

Not built/run per instructions. Hand-verified instead: all three switch(model_) statements now exhaustive and still default:-less; no new includes needed (<cstdint> already present); no narrowing (float literals only); stepped the DivergenceCounterObservable drive by hand for both Aizawa and Lorenz, confirming the guard fires with no intermediate overflow and post-reset output stays finite.

Note (not mine, not touched): two untracked junk files at repo root ('1e-6)', 'current') present before this task's first edit.

### T008 — status: done

**Files written:**
- `f:\projects\iterum\dsp\tests\portability\chaos_enum_exhaustiveness.cpp`

Created the out-of-suite -Wswitch warning-gate TU. Ran both WSL gates from repo root: g++ and clang++ with -std=c++20 -Wall -Wextra -Wswitch -Werror -fsyntax-only - BOTH exit 0 against the current T006/T007 headers.

Anti-vacuity proof (no repo files touched): copied chaos_waveshaper.h into scratchpad with both case ChaosModel::Aizawa: lines stripped, re-ran g++ -> exit 1 with exactly the two expected -Werror=switch errors (updateAttractor, resetModelState). Shadow copy deleted afterward.

DEVIATION: the tasks.md command block was initially pasted verbatim into the file's comment banner including trailing backslash line-continuations, which GCC rejects under -Werror=comment in a // comment; rewritten one command per line, reason stated in the banner.

NOT DONE: step 3 (MSVC build-log diff for zero new C#### warnings) - explicitly deferred to the dedicated build agent per the no-build instruction for this task.

Citation corrections vs tasks.md recorded in the file banner (check-portability.js's flagless g++ invocation and isCheckable() line numbers had shifted by the time this was written).

### T009 — status: done

**Files written:**
- `f:\projects\iterum\dsp\tests\unit\processors\slow_event_scheduler_test.cpp`

Replaced the T001 stub with the 8 required TEST_CASEs, tags [processors][slow_events][vorago]. Every numeric threshold from tasks.md T009 used verbatim. Configuration-ordering rule (all setters before prepare()) enforced by a single applyAndPrepare() helper.

RED STATE VERIFIED: g++ -fsyntax-only gives 'krate/dsp/processors/slow_event_scheduler.h: No such file or directory' - header does not exist yet.

SYNTAX/TYPE VALIDATED without touching the repo: compiled the TU against a throwaway scratchpad stub header matching T010's specified public surface; zero errors/warnings under g++ -Wall -Wextra -fsyntax-only.

TWO DOCUMENTED DEVIATIONS:
1. SC-013's anti-vacuity edge count is measured on an untracked replica (not inside the tracked window), because with the pinned 1s period, 40x processBlock(48000) advances exactly one whole cycle per call, making the tracked window itself show ~5 edges rather than the task's cited 45; the replica reproduces the identical trajectory by construction and yields 45. REQUIRE(risingEdges >= 20u) threshold unchanged.
2. Loop-bound guards inside per-sample advance loops are asserted once via bool-returning helpers instead of a REQUIRE per sample (avoids ~2.3M passing assertions); identical semantics.

Also fixed: the 32-seed table had a duplicate (0x3039 == 12345 decimal, collides with an existing entry) - replaced with 0x2468, reason recorded in-file.

RUNTIME EXPECTATION for the build agent: heavy but not [long]-tagged per tasks.md's tag list; estimated 30-60s in Release.

Unrelated, not touched: three stray untracked repo-root files ('1e-6)', 'current', 'different') flagged for the orchestrator.

### T010 — status: done

**Files written:**
- `f:\projects\iterum\dsp\include\krate\dsp\processors\slow_event_scheduler.h`

Implemented the header-only Layer 2 SlowEventScheduler per tasks.md T010 (single-clock design, smootherstep riseShape, four-draw RNG cycle in order period/target/depth/polarity, FR-067 one-draw pre-roll, fit rule scaled against minIntervalSeconds_, NaN-safe sanitizeClamp, no allocation/locks/exceptions/IO). Did not build with CMake or run Catch2, but syntax-checked and behaviourally probed with WSL g++ -std=c++20 -Wall -Wextra -O2 (zero warnings) against every T009 threshold.

TWO DELIBERATE DEVIATIONS from the tasks.md sketch, both load-bearing (documented in-file):
1. Control update fires at the END of each control step (samplesUntilControl_ = kControlRateInterval on init, decrement-then-check ordering deviated from) rather than the literal sketch, because the literal form makes every onset report one control step late, which would deterministically fail SC-008(e)'s |onsetSeconds - p0| <= 32/48000 clause. processBlock() remains bit-exactly equivalent to N process() calls (verified: processBlock(10,000,000) vs 312500x processBlock(32) identical).
2. getPeriodSeconds() returns a stored periodSeconds_ rather than a periodSamples_/sampleRate_ round-trip, because the round-trip can push a drawn 90s period fractionally outside [20,90] (SC-008(a), zero-tolerance) and breaks SC-008(d)'s exact ==30.0 clause. periodSamples_ remains the timing authority.

Minor: derived time getters return double (not float as an initial sketch might suggest); test call sites use auto/explicit static_cast<double> so either compiles.

Probe results against every T009 threshold reported as passing (pre-roll, SC-008/009/010/011/012/013/018, EdgeCases). No file outside the task's file list touched. Unrelated pre-existing junk observed but not touched: three empty untracked repo-root files ('1e-6)', 'current', 'different').

### T011 — status: done

**Files written:**
- `F:\projects\iterum\dsp\tests\unit\processors\vorago_p1_perf_test.cpp`

Replaced the T001 stub with TEST_CASE("VoragoPhase1_ControlRateCost", "[.perf]"). VoragoPhase1Bundle: 4x PerlinNoiseSource (4 octaves each, distinct seeds) + 4x SlowEventScheduler (distinct seeds) + 1x ChaosModSource in Aizawa mode. Configuration-ordering rule honoured (setIntervalRange/setEnvelopeTimes before prepare()). Two measured passes best-of-5: idle path at FR-052/FR-055 defaults, cycle-inclusive with setIntervalRange/setEnvelopeTimes pinned to their minimums. Single REQUIRE gate on the cycle-inclusive figure only; anti-vacuity WARN guards outside the timed region.

TWO THINGS FLAGGED FOR THE BUILD/VERIFY AGENT:
1. kBaselineNsPerBlock = 7000.0 is CEILING-ANCHORED (largest round value the static_assert against the SC-014 reference budget admits), NOT measured, because this task was instructed not to build/run. After the first [.perf] run, the constant should be replaced with the measured cycle-inclusive figure and the static_assert re-checked.
2. Hoisting risk: tasks.md's mandated read shape (one processBlock(512) per instance, then a separate output-read loop reading each scheduler 512x) means all 512 scheduler reads return the same post-block value from unchanged state, so a compiler could hoist getCurrentValue() out of the loop and the envelope-evaluation cost the read-rate exists to capture might go unmeasured. Implemented the task shape literally; if the measured figure comes back implausibly low, that is the likely cause and needs a task amendment (interleaved process()+read), not a silent deviation.

Verified-by-reading (no compile run) every symbol used against the real headers.

### T012 — status: done

**Files written:**
- `f:\projects\iterum\dsp\tests\unit\processors\vorago_p1_longrun_test.cpp`

Replaced the T012 stub with TEST_CASE("VoragoPhase1_LongRunResolution", "[processors][vorago][long]"). Two 8h Perlin renders (kDefaultRate and kMaxRate, 4 octaves) at 48kHz - hour1/hour8 at control-step granularity, hours 2-7 in coarse blocks; clauses (a) RMS ratio and (b) zero-crossing-rate ratio each within 20%, guarded against a frozen-accumulator NaN case. SlowEventScheduler at pure defaults: onset-count ratio within 20%, anti-vacuity >=10 events/hour, final period in [20,90], 900s-window RMS liveness. Clause (c) non-finite checked inline per advanced block via detail::isFinite (never std::isnan), nothing buffered.

TWO DEVIATIONS, both documented in-file:
1. The four 1h windows use streaming accumulation (sum-of-squares + sign-change count) rather than buffered arrays - arithmetically identical, stricter on the 'never buffer 8h' constraint.
2. Scheduler hour boundaries come from the accumulated sample counter rather than an exact block split (172,800,000 samples is not a multiple of 4096), so a scheduler hour edge can land up to one block (85ms) off; the two Perlin windows ARE exact via static_asserts.

API verified against the real headers this session (getPeriodSeconds() returns double, not float as an earlier draft of the task text implied). Not built/run per instructions; file re-read after writing, no obvious compile hazards found.

### T013 — status: done

**Files written:**
- `F:\projects\iterum\dsp\tests\unit\processors\vorago_p1_harness.cpp`

Implemented TEST_CASE("VoragoPhase1_TrajectoryHarness", "[.harness][processors][vorago]") replacing the T001 stub. Writes exactly 5 CSVs into VORAGO_P1_HARNESS_DIR (3 Perlin octave renders 1/2/4 @60s, Aizawa @60s, SlowEventScheduler @900s), then re-opens each and validates header string, every field parseable+finite, column width, row count, covered duration within one control step, plus scheduler content (rising edges, targeted rows) and an exactly-five-regular-files directory count.

TWO STATED DEVIATIONS:
1. Used std::fixed << std::setprecision(6) rather than bare setprecision(6), because bare setprecision is 6 SIGNIFICANT digits and would round the 900s file's last timestamp with an error exceeding the one-control-step tolerance the test itself asserts - the spec text as literally written was self-contradictory; std::fixed resolves it exactly. Rationale commented in-file.
2. Rows are 1-based (written after each advance) so the final row lands exactly on the stated duration; row counts and the timestamp formula unchanged from the spec.

Other choices: per-field REQUIRE replaced by once-per-file counters (the 900s file holds 5.4M fields); std::strtod not std::from_chars (older AppleClang libc++ lacks FP from_chars); pre-existing vorago_p1_*.csv files removed from the output dir before rendering so the 'exactly five' count describes this run only (the directory itself and any foreign file is never removed). FR-082 carried by the leading-dot [.harness] tag alone.

Not built/run, nothing committed, no file outside the task list touched. Flagged for the build agent: must verify dsp_processors_tests.exe "[.harness]" exits 0 and produces the five files, and that a default run after deleting the dir writes nothing.

### T014 — status: done

**Files written:**
- `F:\projects\iterum\dsp\lint_all_headers.cpp`

Added exactly 2 include lines to the Layer 2 block of dsp/lint_all_headers.cpp: perlin_noise_source.h and slow_event_scheduler.h, each in alphabetical slot. git diff --numstat confirms 2 insertions / 0 deletions, nothing else touched.

Deviation from the literal (elided) snippet in tasks.md: placed slow_event_scheduler.h in strict alphabetical order (after sidechain_filter.h, before spectral_distortion.h) per the section's stated binding rule, rather than the snippet's apparent adjacency to sample_hold_source.h; flagged as a one-line move if the reviewer wants the literal snippet position instead.

No test file written - T014 is lint-TU wiring, verified by run-clang-tidy.ps1 -Target dsp which the dedicated build agent runs after this group. brownian_drift.h deliberately NOT added (explicit out-of-scope). Both included headers confirmed to exist on disk. Nothing built, run, or committed.

### T015 — status: done

**Files written:**
- `F:\projects\iterum\tools\lint-nonfinite-symbols.js`

Added the eight Vorago Phase 1 paths to GUARDED in tools/lint-nonfinite-symbols.js under an in-array marker plus a matching prose banner. vorago_p1_harness.cpp deliberately excluded per the task section. No other file touched.

Verified (node only, no build/test-suite run): node tools/lint-nonfinite-symbols.js prints 'all clear (17 guarded files)' and exits 0.

Non-vacuity checked rather than assumed: none of the eight files is exempted via set_source_files_properties(-fno-fast-math) in dsp/tests/CMakeLists.txt, so the scan actually runs; confirmed by testing a scratchpad probe TU (copy of vorago_p1_perf_test.cpp + injected std::isnan) against a scratchpad copy of the script - gate reported 1 violation and exit 1. Probe removed; git status shows no residue.

Note for orchestrator: five stray untracked repo-root files observed ('1', '1e-6)', '20.0f', 'current', 'different') - not created or deleted by this task.

### T016 — status: blocked

**Files written:** none

BLOCKED BY DESIGN, NOT BY A DEFECT. T016's entire content is 'rebuild the 5 targets and rerun them', which directly conflicts with this session's 'do NOT build or run tests' instruction (a dedicated build agent runs after this group). Did not run the build; the build agent must run T016's exact command verbatim.

Did instead a full static pre-flight of the five suites:
1. -Wswitch exposure fully contained: swept every ChaosModel-switching file (28 files matched the enum name); only the five switches in the two modified headers exist, and all five already carry case ChaosModel::Aizawa. Plugin consumers (Disrumpo/Ruinae) only static_cast or clamp to 0..3, so Aizawa is unreachable from either plugin; one tool has its own unrelated local ChaosModel enum.
2. chaos_waveshaper_test.cpp stays green unmodified (not in git status); its enum-value-count case only asserts the four original ordinals, no exhaustiveness/count assertion exists to trip on a 5th value. COSMETIC STALENESS not fixed (T016 forbids editing that file): the test case's own name text says '4 values' though the enum now has 5 - green but misleading, flagged for a follow-up outside this phase.
3. chaos_mod_source.h's new sampleRate_ floor and divergenceResetCount_ zeroing are inert for every existing test - grepped all consumer test files for prepare(0/prepare(- /getSampleRate, zero hits.
4. Compile-plausibility cross-checked symbol-by-symbol for the new sources (layer discipline, includes, base-class overrides, constants) since compilation could not be run directly.
5. lint-nonfinite-symbols.js will not false-positive on the new files (comment-only isnan/isinf mentions; numeric_limits::max() use in vorago_p1_perf_test.cpp is in the script's existing exemption list).

TWO GAPS IN T016 AS WRITTEN, flagged for the build agent:
a) vorago_p1_perf_test.cpp is tagged [.perf] and vorago_p1_harness.cpp is tagged [.harness] - both hidden from the default run; 'All tests passed' from a default invocation is NOT evidence for the perf/harness requirements and needs explicit tag-filtered runs.
b) vorago_p1_longrun_test.cpp is tagged [long] and runs by default locally, simulating 8h renders twice plus the scheduler - not a failure, but budget the wall-clock time.

REPO POLLUTION flagged, not touched (outside this task's file list): five stray zero-byte untracked repo-root files ('1','1e-6)','20.0f','current','different') that will contaminate the phase commit unless removed; also dsp/tests/portability/chaos_enum_exhaustiveness.cpp is untracked and in no CMake target (inert for the five suites, but currently an unbuilt/unrun file).

### T017 — status: done

**Files written:**
- `f:\projects\iterum\tools\check-portability.js`
- `f:\projects\iterum\dsp\tests\unit\processors\slow_event_scheduler_test.cpp`
- `f:\projects\iterum\dsp\tests\unit\processors\vorago_p1_perf_test.cpp`
- `f:\projects\iterum\dsp\tests\unit\processors\vorago_p1_harness.cpp`

All T017 gates now green: check-portability.js, lint-layers.js, lint-odr.js (738 defs), lint-nonfinite-symbols.js (17 guarded files), lint-float-bit-goldens.js (1483 files) all exit 0; T008's g++/clang++ -Wswitch -Werror WSL commands both exit 0 (re-run this session); run-clang-tidy.ps1 -Target dsp reports 319 files, 0 errors, 0 warnings.

DEVIATION: T017 was specced as verification-only with an empty file list, but two gates were RED on first run and were fixed rather than reported as a red phase.

(1) check-portability.js exited 1: vorago_p1_harness.cpp hit its own #error guard because the script passed no VORAGO_P1_HARNESS_DIR compile definition for that TU (unlike the CMake target, which sets it). Fixed by adding the precedent-matching sibling clause (mirroring the existing KRATE_DSP_AETHER_TEST_HOOKS handling) in pluginFlagsFor() for files under dsp/tests/unit/processors/.

(2) clang-tidy was NOT clean - no prior log existed, so the gate was run directly: 6 warnings, ALL in this phase's new TUs (none pre-existing). Fixed:
- slow_event_scheduler_test.cpp (x2): readability-simplify-boolean-expr. The literal DeMorgan rewrite would have silently BROKEN both NaN guards (De Morgan's form does not trap NaN the way the original guard did); fixed instead by hoisting a named predicate that preserves semantics exactly.
- vorago_p1_perf_test.cpp: performance-enum-size -> explicit std::uint8_t underlying type.
- vorago_p1_perf_test.cpp: readability-redundant-member-init.
- vorago_p1_harness.cpp: modernize-use-integer-sign-comparison -> hoisted a signed constant to avoid a signed/unsigned comparison.
- vorago_p1_harness.cpp: modernize-use-starts-ends-with -> use std::string::starts_with.
All three edited TUs re-verified through check-portability.js and the full clang-tidy re-run is 0 errors / 0 warnings.

ACTION REQUIRED BY THE BUILD AGENT: these edits landed in three test TUs AFTER T016's build/test run, so dsp_processors_tests is stale relative to T017 and must be rebuilt and rerun; assertion semantics are unchanged by design but T016's green result no longer covers the current tree.

Also flagged (not touched, per the no-auto-delete rule): five stray untracked junk files at the repo root ('1','1e-6)','20.0f','current','different').

## Cross-Cutting Repo Hygiene Flag (from task notes, not scored as a compliance item)

Multiple task agents (T002/T003/T005/T009/T015/T016/T017) independently observed and did **not** touch the same set of stray, zero-byte, untracked files at the repo root: `1`, `1e-6)`, `20.0f`, `current`, `different`. These are almost certainly shell-redirect accidents from earlier tasks in this phase, not repo content. They carry no extension, so no lint gate scans them, but `git add -A` (never used in this phase per policy — files were added by explicit path) would have picked them up. **These must be removed (or confirmed intentional) before the phase commit.**

Also flagged: `dsp/tests/portability/chaos_enum_exhaustiveness.cpp` (T008) is untracked and deliberately in no CMake target (it is a manual -Wswitch exhaustiveness gate, run directly via g++/clang++ under WSL, not through CTest) — inert for the five DSP test suites but currently unbuilt/unrun by any automated target. It must still be `git add`-ed for the commit since it is a real, intentional deliverable (T008), not junk.

## Remaining Gates for the Human Loop

- **clang-tidy** — Already run and green within this compliance work: `./tools/run-clang-tidy.ps1 -Target dsp -BuildDir build/windows-ninja` → `Files analyzed: 319, Errors: 0, Warnings: 0` (SC-016, T017). The compile_commands.json used for that run was regenerated this session because the checked-in one was stale relative to the six new TUs — re-verify this gate is still green if further edits land after this report, since a stale cache would silently skip new/changed files.
- **pluginval** — Not applicable to this phase. Phase 1 is DSP-library-only (`dsp/include/krate/dsp/processors/` and `dsp/include/krate/dsp/primitives/`); no plugin source, VST3 parameter, or plugin CMake target was touched (confirmed: `git status` shows only `dsp/` and `tools/` paths modified, per FR-035 and CC-layers evidence above). Plugin integration for Vorago begins at phase 11+ per the project's roadmap phase numbering — pluginval applies from that point forward, not here.
- **Commit** — Not yet done. Before committing: (1) remove or account for the five stray junk files at the repo root listed above, (2) confirm `dsp/tests/portability/chaos_enum_exhaustiveness.cpp` is staged (it is a real deliverable, untracked by git, not yet added), (3) re-run the affected suites one more time if any file changes after T017's fixes (the T017 agent flagged that its own edits landed after T016's build/test pass, so `dsp_processors_tests` should be rebuilt/rerun immediately before the commit to cover the final tree state).
