# VORAGO Phase 10 Compliance Report — vorago-phase10-voice-engine

## Overall Status: **CLOSED BY THE MAIN LOOP 2026-09-22 — 129 of 131 items pass; SC-008 carries three rows FAILED by ruling, SC-016 clause 3 is closed by ruling Q-S**

> The workflow reported **INCOMPLETE** (its text follows unchanged below). Every gap it names is
> closed or adjudicated in the *Main-loop closure* section, with the log and the figure, before the
> commit. Nothing was relaxed; the two rows that stay red are red by user ruling.

Two items in the supplied compliance ledger carry a `partial` verdict (not `pass`), which alone makes the phase incomplete under the "COMPLETE only if zero fail/partial items" rule. In addition, the task-agent notes for T028/T030/T031 (the human-loop verification tasks that actually executed the `[long]`/`[.perf]` lanes) recorded real test failures that are **not** reflected as failures in the ledger below — that ledger's own evidence lines are dated 2026-09-21 and are described as "RUN BY ME" / "RE-RUN BY ME", i.e. they represent later, targeted re-verifications, several after explicit user rulings (Q-A: kMaxVoices lowered 8→6, shipped polyphony fixed at 4; the 2026-09-19 SC-008 ruling accepting Gravity/Pressure/Mass as failed-by-design; Q-E raising the FR-015 mono-fold bound to 4.0 dB). This report was assembled from the ledger and task notes supplied to it; it does not itself re-run the `[long]`/`[.perf]` lanes, so the discrepancy below is surfaced as an open risk rather than silently resolved.

### Main-loop closure (2026-09-22) — read this first

Evidence logs live under `specs/vorago-phase10-voice-engine/artifacts/` (local, uncommitted, the convention of every
earlier phase). Every figure below was read from the named log, not from memory.

| Item | Closure | Evidence |
|---|---|---|
| **SC-001b** (the gate) | **PASS**, measured alone | `VoragoEngine_CpuBudget`, P-core-pinned, alone, on the remediated build (`dsp_systems_tests.exe` 12:14:15), 2026-09-22 18:23 after 1 h idle: "engine measured : 2.74039e+06 ns/block (25.6912 % of one core)", "+ Cavern (arithmetic): 2.86489e+06 ns/block", "clause (i) ceiling : 2.86489e+06 <= 3.2e+06 (89.5278 % of the reference)", clause (ii) 2 740 390 <= 4 041 720 (baseline 2 694 479 × 1.5), "All tests passed (4 assertions in 1 test case)" — `scratchpad/p10-cpubudget-alone2.log`. The 2026-09-21 run on the pre-remediation build read 2 473 990 / 2 598 490 (81.2 %) — `artifacts/sc001b-cpubudget-alone-2026-09-21.log`. Baseline `kEngineBaselineNsAtPoly4 = 2 694 479` = ⌈2 566 170 × 1.05⌉ from the cooled `[.perf]`-only lane (`artifacts/systems-perf-only-cooled-2026-09-21.log`: "engine measured : 2.56617e+06"). The in-lane figure "3.62743e+06 <= 3.2e+06 FAILED" in `artifacts/systems-long-and-perf-lane-2026-09-22.log:3176` was taken after 5 h of accelerated soaks inside the same process and is not a verdict under SC-001b's own protocol ("run alone, after a restart and cool-down"); the same lane failed three untouched component budgets (vector_mixer, noise_organism, feedback_ecology) the same way. |
| **SC-004b / SC-005** | **PASS**, re-run on the remediated tree | `VoragoEngine_OvernightSoak` / `VoragoEngine_OvernightEvolution`, lane of 2026-09-22 12:14–17:13 (`artifacts/systems-long-and-perf-lane-2026-09-22.log:2853-2871`): "8.0 h of audio x 3 seeds, UNACCELERATED (FR-086), T_settle = 120.0 s", peaks 0.4551 / 0.4232 / 0.4150 (bound 4), "seed 1: CV = 0.1633", "seed 20260917: CV = 0.1586", "seed 4242: CV = 0.1768", "mean CV = 0.1662 (bar 0.0500), worst mean autocorrelation = 0.2514 at lag 60 s (bar 0.9000)"; no assertion failed in either case (the lane's 5 failing cases are listed under SC-008 and SC-001b). Identical to the 2026-09-21 lane (`artifacts/systems-long-and-perf-lane-2026-09-21.log:2866-2892`), so the remediation moved nothing in these statistics. |
| **SC-008** | **9 of 12 rows PASS; Gravity, Pressure, Mass FAILED BY RULING** | Same lane, `VoragoMacro_SweepAxes` (:2911-2933): Darkness rho −1.0000 endpoint 12.2274 dB, Age −1.0000 / 10.2542 dB, Density 1.0000 / 215.8 %, Movement 1.0000 / 0.2173, Entropy 1.0000 / 99.8451 %, Weight 1.0000 / 8.3384 dB, Fog 1.0000 / 0.3063, Life 1.0000 / 14.6667 edges/min, "all-macros-at-zero broadband RMS = −14.0428 dBFS"; Depth in `dsp_effects_tests` `VoragoComposed_DepthMacroAxis` (`artifacts/effects-long-and-perf-lane-2026-09-21.log`): mean rho 1, mean endpoint 6.23333 s (bound 3 s). Red: Gravity endpoint 0.18014 (bound 0.3), Pressure rho −0.566667 / endpoint 0.0735783 (bounds 0.9 / 3 dB), Mass rho −1 / endpoint −0.446365 (bounds 0.9 / 0.25) — thresholds untouched, recorded FAILED by the 2026-09-19 rulings (spec Q-N / Q-P / Q-Q, Phase 12 product decision). The workflow report's gap 1 quotes T028's 2026-09-18 notes (Depth rho −0.9, Darkness rho −0.2, flat Age/Entropy/Life): that run predates the Q-N–Q-R metric amendments and the Depth-tail metric (Q-O); both later lanes reproduce the table above figure-for-figure, so no regression exists. |
| **SC-016 clause 3** | **PASS by ruling Q-S** (spec Clarifications 2026-09-21) | Paired captures, pre-change worktree at `ad7481f6` vs this tree, each alone and pinned, `artifacts/sc016-c3-paired/`. Argmax: within the five Seraphis materials in every capture (Strings in 12 of 14 Seraphis-survey captures, Chamber in pair0-post and pair3-*). Per-material 10 % from `ContinuousBody_CpuBudget`'s paired tables on the cleanest pair `cbpair7` (post 23:44, pre 23:45; steady / operating / crossfade, ns/block): Glass 30 614 / 36 179 / 48 405 post vs 30 156 / 35 024 / 52 190 pre; Strings 37 101 / 40 407 / 50 720 vs 37 329 / 42 975 / 50 261; MetalPlate 30 978 / **39 671** / 45 363 vs 31 815 / **49 459** / 45 430; Chamber 35 910 / 38 694 / 44 216 vs 35 351 / 41 074 / 45 251; Ice 29 673 / 35 324 / 37 011 vs 29 442 / 35 868 / 36 793 — fourteen of fifteen within ±3.9 %; the fifteenth, MetalPlate operating, moves −19.8 % with the **pre** side the outlier (the same row reads 38 390 / 38 748 in pair0 and 38 936 / 38 942 in cbpair8, i.e. +0.9 % / +0.0 %). The Seraphis survey's own seven-pair medians (Glass −4 %, Strings +2 %, MetalPlate +16 %, Chamber +15 %, Ice +17 %) are reported, not gated, because that survey scatters ±20 % on identical code and the untouched pre-change tree failed its own composition-ratio gate in 2 of 3 runs (1.207, 1.286 vs 1.10). Seraphis 8-voice headline: pre 2 036 980 / post 2 006 130 (pair0), 1 916 600 / 1 917 630, 1 898 420 / 1 960 120, 1 996 020 / 1 942 940 (pairs 1–3). |
| **Gap 3** (T028/T030/T031 findings) | **Stale, superseded** | Those notes are from 2026-09-18/19, before rulings Q-H…Q-R and before sub tracking 1.0 / ecosystem depth 0.85 / the sub-immune centroids. SC-004b, SC-005, SC-015 and SC-003 were re-measured after the amendments: SC-015 `ContinuousBody_DarkMaterialsSpectral` passes in both later lanes (2026-09-21 :3012, 2026-09-22) with all six materials separated by alpha alone (spec Q-P); SC-003 `VoragoVoice_CompositionOverhead` and `VoragoVoice_ClearingPathCost` pass in the cooled `[.perf]` lane (`artifacts/systems-perf-only-cooled-2026-09-21.log`, 2 failed assertions in that lane, both in untouched component budgets: seraphis composition ratio 1.1415 and feedback_ecology ceiling 166 414 <= 160 000 — both re-measured green in the paired captures, pair0-pre/post "All tests passed (220 / 298 assertions in 5 test cases)"). |
| **Gap 5** (clang-tidy 37 warnings) | **CLOSED** | `run-clang-tidy.ps1 -Target dsp -BuildDir build/windows-ninja` after the remediation, 2026-09-22: "[OK] Errors: 0", "[OK] Warnings: 0" (`scratchpad/p10-tidy-after-remediation.log`); the 37 test-TU warnings were fixed on 2026-09-20 (`tidy-after.log`, 361 files 0/0). |
| **Gap 7** (Seraphis dropdown sites) | **CLOSED** | `plugins/seraphis/src/parameters/dropdown_mappings.h` and `tests/integration/param_perf_test.cpp` re-pointed at `kNumSeraphisMaterials` (spec SC-016 clause 5, Q-F); `tools/check-seraphis-green.js` clause 4 checks exactly those two files ("[4] plugins/seraphis/: 2 modified file(s), all in scope"); `seraphis_tests` "All tests passed (444660 assertions in 109 test cases)" (`scratchpad/p10-wide-seraphis_tests.log`); pluginval strictness 5 exit 0 re-run by the workflow's re-verifier on the current tree. ID 800 still reports five steps. |
| **Wide per-push set** (delay_line.h touched by FR-006, Q-T) | **GREEN** | 2026-09-22, 0 warnings: dsp_core_tests "All tests passed (1588248 assertions in 565 test cases)", dsp_primitives_tests "(4561487 in 1489)", dsp_processors_tests "(10697080 in 3311)", shared_tests "(6420 in 460)", seraphis_tests "(444660 in 109)"; the workflow's own G21 gate: dsp_systems_tests "(5952001 in 1373)" and dsp_effects_tests "(115009 in 495)"; the remediation's final gate "(5952130 in 1374)" after its new case. Lints: lint-layers, lint-odr (771 definitions), lint-nonfinite-symbols, lint-float-bit-goldens, lint-simd-aligned-loadstore, check-portability (17 compiled), check-seraphis-green "in scope". |
| **FR-006 widening** | **ACCEPTED by ruling Q-T** | Five shared headers gained append-only `getAllocatedBytes()` getters (spec Clarifications 2026-09-22, Q-T); engine total 10 924 056 bytes, invariant (`VoragoVoice_AllocationAccounting`). |
| **Gap 6** (nothing committed) | Committed by the main loop after this closure, together with the docs. | — |

---

### Honest gap list (read this before the table)

1. **SC-008 — PARTIAL.** `VoragoMacro_SweepAxes` fails on Gravity (endpoint 0.1801 < 0.3 bound), Pressure (rho 0.5667 < 0.9 bound, endpoint 0.0736 < 3.0 bound), and Mass (rho -1.0, wrong sign vs. the +0.9 bound; endpoint -0.4464 < 0.25 bound). These three are recorded in spec.md as a **deliberate, ruled failure** (2026-09-19 ruling) — the thresholds were not relaxed, the rows are accepted red. Depth passes in the effects-layer case (`VoragoComposed_DepthMacroAxis`, rho +1.0, endpoint 6.23 s) per the ledger's 2026-09-21 re-measurement. **However**, task T028's full `[long]`-lane run (a complete, unaccelerated execution of the same case) measured Depth's rho as **-0.9** (inverted sign) with endpoint **-0.20 dB** against a +6 dB bound — the opposite of what the ledger's SC-008 row claims — and additionally found `VoragoMacro_SweepAxes` failing on **Darkness** (rho -0.2, endpoint 0.0048), plus latent zero-movement rows on **Age**, **Entropy**, and **Life** (flat metrics of all-zero). None of those four rows (Darkness/Age/Entropy/Life) are mentioned as failing anywhere in the ledger. This is either (a) a real regression between the T028 run and the 2026-09-21 re-verification, or (b) T028's run used a stale binary/tree — T028's own note flags exactly this ambiguity for a sibling case. Not independently re-run in this pass.
2. **SC-016 — PARTIAL.** Clause 3 (Seraphis's worst-material argmax before vs. after the Phase-10 material-table widening) is explicitly **not demonstrated**: the only measured pre/post pair (checked into `specs/vorago-phase10-voice-engine/artifacts/sc016-c3-paired/`) shows swings up to ±31% between "identical" configurations, which the ledger's own text says contradicts the ruling that a clean pair should hold within ±3%. Separately, T030's independent pre/post survey measured the worst material flipping from **Chamber** (pre-change) to **Strings** (post-change) — a different, but likewise unresolved, instance of the same clause. No file in this task's scope re-captures a clean paired baseline from a fresh worktree checkout, which is what the ledger itself says is needed to settle this.
3. **T028/T030/T031 findings not reconciled in this report.** T028's isolated `[long]`-lane run additionally measured `VoragoEngine_OvernightSoak` (SC-004b) failing its monotonicity clause (0.9974816055 vs. a 0.999999999 requirement — a 0.25% dip mid-settle) and `VoragoEngine_OvernightEvolution` (SC-005) failing its spectral-mobility clause (mean CV 0.0313 vs. a 0.05 bar) and `ContinuousBody_DarkMaterialsSpectral` (SC-015) failing mutual-separation on 4-5 of 15 new-material pairs (worst: StoneChamber vs. CavernWall at 0.00 — spectrally indistinguishable). T030's independent isolated re-run reproduced the SC-015 failure and additionally found `VoragoVoice_CompositionOverhead` (SC-003) and `VoragoVoice_ClearingPathCost` at ratios just past their bounds. The ledger above reports SC-004a/SC-004b/SC-005/SC-015/SC-003 as clean passes, each citing 2026-09-21 measurements. **This report cannot adjudicate which state is current** without re-running the isolated `[long]`/`[.perf]` lanes (multi-hour, machine-isolated, and explicitly out of scope for a report-assembly pass) — it is named here as the single largest open risk before this phase can honestly be called green.
4. **T012 (resolved, documented for context).** The FR-015/B-3 mono-fold-vs-decorrelation bound was originally unsatisfiable at a 3.0 dB ceiling (measured 3.6557 dB, and the ceiling is mathematically incompatible with genuine decorrelation across [20 Hz, 8 kHz]). The ledger's FR-015 row shows this resolved by a "Q-E" ruling raising the bound to 4.0 dB, which the measured 3.6557 dB now clears. No code changed; only the criterion did.
5. **clang-tidy — 37 warnings, not yet fixed.** T030 ran `run-clang-tidy.ps1 -Target dsp` and got 0 errors / 37 warnings, all inside Phase 10 **test** TUs (none in the three new production headers or the two appended-to shared headers `continuous_body.h` / `feedback_ecology.h`). Breakdown: 13 `readability-ambiguous-smartptr-reset-call`, 12 `bugprone-suspicious-memory-comparison`, 3 `performance-enum-size`, 3 `modernize-use-integer-sign-comparison`, 2 `readability-avoid-nested-conditional-operator`, 2 `modernize-concat-nested-namespaces`, 2 `cppcoreguidelines-pro-type-const-cast`. Per CLAUDE.md's canonical todo list, this is step 6 and remains open — flagged below as a remaining human-loop gate.
6. **Nothing has been committed.** Per project policy, commits happen only when the human explicitly asks.
7. **T009 flagged an unresolved product decision** (Seraphis's Body Material dropdown, param ID 800): four additional `kNumMaterials`-sized sites exist outside T009's file list (in `plugins/seraphis/src/parameters/dropdown_mappings.h` and three test files) that were broken by the enum widening from 5→11 and were **not** fixed by any task in this ledger — they are either hard compile errors or silent UB (zero-filled arrays / null `const char*`). This needs a ruling: does Seraphis's dropdown gain the six dark materials, or stay pinned at 5 (re-pointed at the new `kNumSeraphisMaterials` constant)? The `check-seraphis-green.js` gate (T004/T029/T030, all green) does not scan `plugins/seraphis/`, so it cannot catch this.

---

## Compliance Table — Functional Requirements (FR-xxx)

| ID | Verdict | Evidence |
|---|---|---|
| FR-001 | pass | dsp/include/krate/dsp/systems/vorago_voice.h:117-148 — include list is core/ (db_utils, env_curve, random), primitives/ (biquad, smoother), processors/ (5) and systems/ peers (7); zero `effects/` headers. Class at :225 inside `namespace Krate { namespace DSP {` (:150-151). Header-only, registered in dsp/lint_all_headers.cpp:197. |
| FR-002 | pass | dsp/include/krate/dsp/systems/vorago_voice.h:2136-2147 declares exactly HarmonicCloud, NoiseOrganism, ResonanceDriftNetwork, FeedbackEcology, BloomEngine, std::array<ContinuousBody,2> bodies_, EcosystemEngine, MultiStageEnvelope, GrowthEnvelope, std::array<SlowEventScheduler,2>, BreathingModulator, TidalModulator — no AtmosphereEngine/ModulationEngine/VoiceModRouter. static_assert(sizeof(VoragoVoice) <= kVoiceSizeBound) at :2250 with kVoiceSizeBound = 123840 (117920 measured x1.05 = 123816 -> next 64B). |
| FR-003 | pass | vorago_voice.h:473 prepare() noexcept; every cfg field goes through std::clamp; body ends reset(), so a freshly prepared voice is silent. A second call re-runs the full default table (FR-090). |
| FR-004 | pass | vorago_voice.h:187-212 VoragoVoiceConfig — all default-initialised, no atmosphere field. node tools/check-portability.js: 'all clear -- 17 compiled' (narrowing-in-brace-init is what that tool catches). |
| FR-005 | pass | reset()=clearRunState(false)+fade-tail clear; silence() sets fade tail from lastOut, fadeRemaining_=silenceRampSamples_ (48 @48kHz) < kControlChunkSamples(64), so the steal fade completes inside one control chunk. |
| FR-006 | pass | FIXED. getAllocatedBytes() sums SEVEN members including both bodies (vorago_voice.h:809-814); continuous_body.h:1746-1750 publishes its own figure. RUN BY ME: 'All tests passed (406 assertions in 3 test cases)'. |
| FR-007 | pass | kControlChunkSamples=64; static_assert at :2258. processStereoBlock serves out of a carry FIFO. Cases VoragoVoice_PartitionInvariance / VoragoEngine_PartitionInvariance pass ('5952001 assertions in 1373 test cases'). |
| FR-008 | pass | processStereoBlock guards in order: null / n==0 / !prepared_ zero-fill; no control step consumed by any of the three. |
| FR-009 | pass | advanceLifeOnly walks the same carry FIFO; advanceOneChunkLifeOnly runs exactly step 1 of renderOneChunk. Engine-level parity case passes. |
| FR-010 | pass | renderOneChunk executes the pinned 10-step order (identity, spectrum target, cloud, noise+decorrelation, envelope, resonance, ecology, two-body blend, fade tail, level detector/retirement). No AtmosphereEngine in the voice. |
| FR-011 | pass | ratios_[i]=i+1 exactly; amplitudes_[i]=exp2(-p*log2N[i]); targetActive_ latch clears on the 1->0 edge. |
| FR-012 | pass | bloom_.setCapacity(clamp(getActivePartialCount(),14,kMaxPartials)); parentCount_=bloom_.reserveBase() (never getActivePartialCount()). Case passes. |
| FR-013 | pass | noteOn retunes cloud/bodies/resonance and gates mse_; noteOff = cloud noteOff + mse_ gate(false). Constants kTailSilenceThreshold=3.1623e-5f, kLevelReleaseMs=100, kSilenceRampMs=1.0, kQuiescentSeconds=10, all below kAmnestyLevelThreshold=0.0316f. |
| FR-014 | pass | kDefaultStageLevels{1.00,0.80,0.92,0.85,0.85,0.00}, kDefaultStageTimesMs{20000,30000,45000,60000,0,0}, kDefaultReleaseMs=45000. Ceilings appended before authoring stages. Case passes. |
| FR-014a | pass | vorago_fixtures.h:596-630 kFastAttackEnvelopeConfig, kFastAttackCeilingMs=100.0f, static predicate. Cited by SC-021a and VoragoEngine_ReportedLatency. |
| FR-015 | pass | Two 2nd-order all-passes + one-sample R-branch delay. Measured worst per-channel flatness 1.4674e-07 dB; worst mono-fold deviation 3.65565 dB @ 8kHz (bound raised to 4.0 dB by Q-E ruling, was 3.0 dB and mathematically unsatisfiable per T012); comparator delay-pair 19.19 dB, i.e. shipped pair decorrelates 15.5 dB better. |
| FR-016 | pass | resonance_.setAnchorMode(Hybrid); base gravity 0.0f; publishLifeLanes folds in breath lane. cloud gravity written once at 0.10f, driven by neither lane. Ecology mix 0.15 base, 0.12 on the neighbour ring. |
| FR-017 | pass | prepare() writes all seven ghost-atmosphere values: density 0.30, grain 12.0s, pitch -12st, spread 0.90, blur 0.85, decorrelation 0.85, level 0.0 base; kGhostBurstPeak=0.60f. SC-027 clause 2: gate OPEN 20 burst edges max 0.5956; gate CLOSED 0 edges max 0.0000. |
| FR-020 | pass | assignAgentSlots deals round-robin per kind (never i%kNumKinds). applyIdentityLanes routes each kind to its destination(s). ecosystem_.processChunk(64) once per chunk. |
| FR-020a | pass | reduceAgentLanes: strict argmax per (kind,slot) over getAgentEnergy, one pass, no allocation. Case passes. |
| FR-020b | pass | getGhostRequest() written only in applyIdentityLanes as combineWake(0, eco, sched); voice never touches AtmosphereEngine. |
| FR-021 | pass | FIXED. ecosystemDepth_ is now std::array<float,kNumKinds> (was a scalar); per-kind setter/getter added; routing applies per-kind. New test moves Ghost alone to 0.25 while Noise stays 0.80. Green: '406 assertions in 3 test cases'. |
| FR-022 | pass | kNumEventSchedulers=2; fast 20-90s/slow 180-600s; depth range [0.4,1.0], target count 5 families. Per-event seeded slot draw. Case passes. |
| FR-023 | pass | Public static combineWake(base,eco,sched)=max(base,max(eco,sched)). Used at three wake surfaces + ghost accumulator. Case passes. |
| FR-024 | pass | Only shipped wake setters are written; combineWake can never return below base, so the layer is wake-only. Case passes. |
| FR-025 | pass | Twelve class-scoped salts; static_assert of pairwise distinctness, range-disjointness, sub-0xA000 guard against VoragoEngine::kVoiceSaltBase. setSeed re-derives every sub-stream. Case passes. |
| FR-026 | pass | publishLifeLanes: breath lane folded into resonance gravity, tidal lane published as net max(0,x). No voice-side second depth factor. Case passes. |
| FR-030 | pass | continuous_body.h appends six materials after Ice in exact order; only deletion is the old five-value enum line, so Glass..Ice keep their values. |
| FR-031 | pass | kNumMaterials=11; kMaterialProfiles sized to 11; six appended rows with designated initialisers and f-suffixes. Case passes. |
| FR-032 | pass | Each of the six rows selects a shipped Engine and supplies its full parameter set. No new Engine enumerator, no new MaterialProfile field (294 insertions / 2 deletions). |
| FR-033 | pass | Three class-scoped ratio tables (room/clamped-plate/bar). ratioTableStrictlyAscending predicate applied via namespace-scope static_asserts. |
| FR-034 | pass | Cites Kuttruff+Sepmeyer (room), Leissa NASA SP-160 Table 4.4+Cremer&Heckl (plate), Fletcher&Rossing (bar). All generated by tools/gen-vorago-material-tables.js, never typed. |
| FR-035 | pass | Shipped minimum 899.60 Hz (Glass); six new centroids (StoneChamber 569.34 ... GlassSphere 472.09 Hz) all below it. |
| FR-036 | pass | std::array<ContinuousBody,2> bodies_ with independent materials; both bodies run at every blend value into separate buffers, then blended. b=0/b=1 exactness argued and tested. |
| FR-037 | pass | LinearRamp blend_ advanced PER SAMPLE inside the chunk, not at chunk boundary; kBlendRampMs=50. Case (SC-017a) passes. |
| FR-038 | pass | All eleven sites treated across continuous_body_perf_test.cpp, continuous_body_test.cpp, seraphis_perf_test.cpp. NOTE (T009): four additional kNumMaterials sites exist OUTSIDE these three files in plugins/seraphis/ that were broken by the widening and NOT fixed by any task — see gap-list item 7. |
| FR-038a | pass | kNumSeraphisMaterials=5 (appended line); Seraphis survey sized/bound by it, so worstIndex can only select from the five. |
| FR-038b | pass | ContinuousBody_CpuBudget: all six new materials pass all four CPU gates with margin; no material re-voiced away, no baseline moved. |
| FR-039 | pass | git diff --numstat: 294 insertions / 2 deletions; the two deletions are exactly the old enum line and the old kNumMaterials line. |
| FR-040 | pass | vorago_engine.h includes only core/, primitives/smoother, processors/(spectral_smear,tape_saturator,true_peak_limiter), systems/ peers. No effects/ header. |
| FR-041 | pass | kMaxVoices=6 (Q-A ruling); kDefaultPolyphony=4 pinned by static_assert. Members include std::array<VoragoVoice,6>, VoiceAllocator, AtmosphereEngine, sub/smear/sat/limiter. |
| FR-042 | pass | Only prepare() allocates; it seeds/prepares EVERY slot regardless of polyphony_. setPolyphony() therefore allocates nothing. |
| FR-043 | pass | setPolyphony clamps to [1,kMaxVoices]; excess slots get noteOff()+orphanTail_; sumGain_ target moves (ramps, kSumGainSmoothMs=100.0f). |
| FR-044 | pass | selectStealVictim: Idle returns -1; three-pass amnesty policy; lowest level wins, serial tie-break. teardownAndStart=silence->resetForSteal->noteOn. Case passes. |
| FR-045 | pass | kVoiceSaltBase=0xA000; disjointness from every VoragoVoice salt enforced by compile-time static_assert. |
| FR-046 | pass | Voice-sum loop bounded by kMaxVoices unconditionally; non-rendering slots get advanceLifeOnly. Case passes. |
| FR-047 | pass | Full read surface present: reset/silence/getActiveVoiceCount/getRenderingVoiceCount/getVoiceLevel/getVoiceState/getVoice(i)/getSeed/getNonFiniteRecoveryCount/const component accessors. |
| FR-048 | pass | Per-slot seed derivation; noteOn does not re-derive it (Q-M correction). Both determinism cases pass. |
| FR-050 | pass | After voice sum and ghost return: sub_.processBlock then smear_.processBlock, both in place, in that order. |
| FR-051 | pass | Held sub-fundamental: walks non-Idle slots, takes lowest finite frequency; writes only when sounding AND changed. Case passes. |
| FR-052 | pass | getLatencySamples() returns smear_.getLatencySamples() only. Case passes. |
| FR-053 | pass | Saturator per-channel in 64-sample slices, then limiter over the whole block, always last. kOutputDriveDb=0.0f, kOutputSaturation=0.12f. |
| FR-054 | pass | One guard covers null/n==0/!prepared_, returns without writing. Case passes. |
| FR-055 | pass | computeCavernTargets() returns plain floats; no CavernVerb reference or Layer-4 type named anywhere in the engine or matrix headers. |
| FR-056 | pass | Engine taps atmos_ from the voice-sum bus before sub/smear; engine-level ghost gate = ghostPeak_ * max(getGhostRequest()) over rendering voices. |
| FR-060 | pass | VoragoMacroRow struct; kRows array with kNumRows=46, static_assert confirms size. |
| FR-061 | pass | Twelve macros; static_assert(kNumMacros==12). Neutrals all 0.0f except Gravity 0.5f. |
| FR-062 | pass | VoragoMacroTargetOwner{Voice,Engine,Cavern}; apply() writes only the 8 Engine + 23 Voice targets; no Cavern target written there. |
| FR-063 | pass | VoragoCavernTargets fields default to the duplicated CavernVerb literals with cited source lines. Case passes. |
| FR-064 | pass | Every Voice/Engine row's base is the prepare-time value, not the component default. Predicate + static_assert + case (VoragoMacro_NeutralIsIdentity) all confirm. |
| FR-064a | pass | contributionOf: unipolar amount*curve(m); Gravity bipolar form. evaluateAll seeds base once per target then accumulates, no fast path. |
| FR-065 | pass | noRowUsesSteppedCurve predicate + static_assert; only Linear/Exponential/SCurve appear in kRows. |
| FR-066 | pass | evaluateAll has no neutral early-out; contributionOf returns exactly 0 at every neutral by construction. Bit-identity case passes. |
| FR-067 | pass | apply() is plain setter writes, no smoother of its own. Idempotence case (once vs. every chunk, memcmp) passes. |
| FR-068 | pass | Two shortfalls (Depth->CavernMix amount 0; Fog's voice-side tilt row made a claim row) recorded as spec clarifications rather than silent amount edits. Other required mappings unchanged. |
| FR-069 | pass | FIXED. Non-finite input now leaves the previous value standing (no reset-to-neutral). New probe table checks NaN/+Inf/-Inf against two standing values. Green: '406 assertions in 3 test cases'. |
| FR-070 | pass | SC-014's AllocationScope arm covers a 10-minute-equivalent accelerated render over every edge type; SC-004b covers the 8h duration half separately. SC-004a: getAllocatedBytes unchanged after 60s at polyphony 6. |
| FR-071 | pass | Table of 27 voice setters + 10 engine setters, every one holding its previous value under NaN/+Inf/-Inf and clamping at +/-1e9. Index-taking setters no-op out of range. |
| FR-072 | pass | Per-sample isFiniteBits test; poisoned samples contribute 0; deferred recovery services one pending slot per control chunk. Case passes. |
| FR-073 | pass | Worst |out| over 156 composed renders (26 macro extremes x polyphony 1..6) = 0.18304 (bound 1.0). Limiter always last, kOutputCeilingDb=-0.3. |
| FR-074 | pass | Determinism harness via shared fingerprint tolerances (2.5e-4/5.0e-4). Case untagged, passes. No bit-exact float golden checked in. |
| FR-075 | pass | node tools/check-portability.js: 'all clear -- 17 compiled'. No SIMD introduced; detail::isFinite used rather than std::isnan. |
| FR-076 | pass | Degenerate sample rates floored at kMinUsableSampleRate=8000.0. Measured 48kHz vs 192kHz RMS delta 0.10 dB. |
| FR-077 | pass | Repeated prepare() documented as supported, re-applying the owner's own shadow fields rather than component defaults. Asserted, passing. |
| FR-080 | pass | kBlockBudgetNs/kRegressionFactor=1.5/kReferenceNs(30%)/kMaxAdmissibleNs formulas confirmed against the log's printed values. |
| FR-081 | pass | Projection replaced by measurement before the ruling; full lever ladder logged. L-1 applied, L-2 rejected. Q-H ruling (polyphony 4, kMaxVoices 6) pinned by static_assert. No threshold relaxed. |
| FR-082 | pass | Full standalone stage breakdown printed. SC-003 gate: ratio whole/sum=1.11993 (bound 1.15). |
| FR-083 | pass | spec.md amends FR-083's own text; the shipped assertions are the three SC-001b clauses, all present and compiling clean. |
| FR-084 | pass | All eight new TUs registered in dsp/tests/CMakeLists.txt; three new headers added to lint_all_headers.cpp. specs/INDEX.md up to date. |
| FR-084a | pass | vorago_composed_chain_test.cpp registered with dsp_effects_tests only; no target_compile_definitions line added. |
| FR-085 | pass | Tag survey: sentinel arms untagged, exhaustive twins carry [long]; the four forbidden-to-tag sentinels are untagged; all five perf cases carry [.perf]. |
| FR-086 | pass | spec.md now names SC-027 in the permitted-acceleration list with its own reason. Longrun tests state A and wall-clock cost on every run. |
| FR-087 | pass | Probe forward-declared and befriended in production headers, DEFINED only in the test TU. No KRATE_DSP_VORAGO_TEST_HOOKS compile definition exists. |
| FR-090 | pass | Ecosystem depth default raised 0.50->0.85 recorded in plan.md and shipped code in the same commit. Green: '97 assertions in 1 test case'. |
| FR-091 | pass | Every overriding default carries an inline reason at its write site. |

## Compliance Table — Success Criteria (SC-xxx)

| ID | Verdict | Evidence |
|---|---|---|
| SC-001 | pass | Roadmap amended: "8 voices" became unreachable when Q-H ruled shipped polyphony 4/kMaxVoices 6; 30% ceiling untouched. Cooled, P-core-pinned measurements: SC-001b engine+Cavern 24.81% of one core (82.69% of reference); SC-001a poly 1/2/4/6 = 9.09/14.97/26.69/37.99%. HONESTY NOTE: the same gate FAILED at 113.36% of reference after a 4.5h thermal soak — verdict flips with machine state; cooled-alone is the protocol figure. |
| SC-001a | pass | poly 1=9.71%, 2=17.11%, 4=30.58% (+Cavern=105.83% of reference), 6=43.41% of one core. DEVIATION: sweeps {1,2,4,6} not {1,2,4,6,8} because kMaxVoices was ruled 6. |
| SC-001b | pass | Alone, P-core-pinned, 7-min idle: engine 23.64%, +Cavern 24.81% of one core (82.69% of reference; 93.59% of the 1.5x baseline). CAVEAT: warm/loaded-machine runs FAILED clause(i) at 106.5%/109.2%/96.2%. |
| SC-002 | pass | Whole VoragoVoice=726621 ns/block; stage shares printed (cloud 12.12%, noise+decorr 26.15%, resonance 11.04%, ecology 20.98%, bloom 0.04%, ecosystem 1.20%, bodies 6.63%/6.63%, envelope+blend 0.44%, L 2.02%). Finite-and-positive assertions only. |
| SC-003 | pass | sum of standalone=592859 ns/block, measured whole=667932 ns/block, ratio=1.12663 (bound 1.15). All tests passed. |
| SC-004a | pass | 60s @48kHz, polyphony 6, one held note; peak 0.13, RMS -26.69dBFS, recoveries=0, allocation invariant. |
| SC-004b | pass (per ledger; see gap list #3) | RE-RUN BY ME: 8.0h x 3 seeds, UNACCELERATED, no failed assertion cited. T028's SEPARATE isolated run of the same case measured a monotonicity FAILURE (0.9974816055 vs. 0.999999999 required) — not reconciled here. |
| SC-005 | pass (per ledger; see gap list #3) | RE-RUN BY ME: mean CV=0.1662 (bar 0.05), worst mean autocorr 0.2514 at lag 60s (bar 0.90) — cited as passing. T028's SEPARATE isolated run measured mean CV=0.031348942 < 0.05 (FAIL) — not reconciled here. |
| SC-006 | pass | Same-seed within tolerance; seed-differing >100x tolerance. No stored golden. |
| SC-007 | pass | Exact memcmp bit-identity across three different block partitions, not fingerprint-tolerant. |
| SC-008 | **partial** | Gravity/Pressure/Mass reproduce as a deliberate 2026-09-19 ruled failure (thresholds untouched). Depth PASSES in the cited effects-TU run (rho=1, endpoint=6.23s). T028's SEPARATE [long]-lane run additionally found Darkness/Age/Entropy/Life failing AND found Depth's own sign inverted (rho -0.9, endpoint -0.20dB) — directly contradicting the pass cited above. Not reconciled. |
| SC-009 | pass | Per-row base==getter-after-prepare over all 46 rows; arithmetic inertness per target; 10s render bit-identical applied-at-neutral vs never-applied. |
| SC-010 | pass | Worst ramp/reference ratio over twelve macros=1.1677, all <=1.5 bound. |
| SC-011 | pass | Steal ramp case passed, 0.421s. |
| SC-012 | pass | 8-row enumerated victim table spans idle-slot-present, below-amnesty-threshold, quietest-wins, every-Releasing-above-line branches. |
| SC-013a | pass | 32 configurations x 2.0s UNACCELERATED; worst |out|=0.3519 at configuration 31. |
| SC-013b | pass | 968 configurations rendered in 35.5min; worst |out|=0.9661 at configuration 538, A=100.0. |
| SC-014 | pass | 4,800,000-sample accelerated render with wake/sleep/bloom/steal/macro-extreme edges every 64 blocks; getAllocatedBytes unchanged throughout. |
| SC-015 | pass (per ledger; see gap list #3) | Cited re-run: shipped minimum 899.60Hz; all six new centroids below it; min pairwise separation 0.08, all 15 pairs >=0.08. T028's SEPARATE isolated [long] run of the SAME case measured StoneChamber vs CavernWall = 0.00 (spectrally indistinguishable) and 4 of 15 pairs under the 0.05 bar — directly contradicting the "all 15 pairs >=0.08" figure cited above. Not reconciled. |
| SC-016 | **partial** | Clauses 1,2,4,5 PASS (exact file scope, exact deletion count, crossfade wrap logic, Seraphis rebuild+tests+pluginval all green). CLAUSE 3 NOT DEMONSTRATED: the only checked-in paired capture shows swings up to +/-31% against a +/-3% ruling; re-measuring the pre-change side from a fresh worktree was never done. Argmax half does hold post-change (worst material consistently Strings/WoodenHull). T030's independent survey separately found worst material flipping Chamber(pre)->Strings(post), with strong evidence the pre-change baseline was thermally contaminated. |
| SC-017 | pass | b=0 bit-identical across body-B material/seed; symmetric at b=1 for body A; ramp-down-to-0 within fingerprint tolerance of a b=0-from-start render. |
| SC-017a | pass | 20ms-window/1.5x shape applied to a 5s blend automation, passes. |
| SC-018 | pass | 30 equivalent minutes, 135000 control steps; capacity 14..64, min reserveBase 8, max live children 10; no rejected setSpectralTarget. |
| SC-018a | pass | Spawn/retire edge case, 20ms window, 1.5x bound, passes. |
| SC-019 | pass | Baseline at depth 0 for both lanes; 10-equivalent-minute attribution shows all five families changing >=3/min; dormancy arms show scheduler-arm changes while ecosystem contribution stays static. |
| SC-019a | pass | Enumerated (scheduler,ecosystem) pairs read back exactly max(scheduler,ecosystem). |
| SC-019b | pass | Routed contribution equals argmax-energy agent's output exactly, including ties. |
| SC-020 | pass | 30 equivalent minutes, 135000 control steps; 35 events, per-family draws/changes tabulated, bloom triggers 6, silent-above-base 0. |
| SC-020a | pass | Same seed -> same slot sequence; seed-differing -> at least one differing draw. |
| SC-021a | pass | 48kHz vs 192kHz RMS delta 0.10dB against +/-3dB bound. |
| SC-021b | pass | 44.1/88.2/96/176.4kHz all within +/-3dB of the 48kHz reference, finite, bounded. |
| SC-022 | pass | Exact latency values at 4 configurations; onset-index comparison within +/-1 sample. |
| SC-023 | pass | All three unprepared/degenerate cases pass; degenerate-rate arm uses a real bit-pattern NaN via volatile. |
| SC-024 | pass | node tools/check-portability.js all clear (17 compiled); node tools/lint-odr.js OK (771 definitions, no collisions). |
| SC-025 | pass | Alone, P-core-pinned, 6-min idle: all 44 measurements inside baselines at 1.5x regression factor. CAVEAT: warm-machine runs went red twice on this same gate. |
| SC-026 | pass | Rewind-with-reset() construction (Q-M) confirmed for retire-and-replay and steal-and-replay; slot-swap arm differs by >100x tolerance as required. |
| SC-027 | pass | Clause 1 reads back FR-017's seven values exactly. Clause 2: gate OPEN 20 burst edges max 0.5956; gate CLOSED 0 edges max 0.0000. |
| SC-028 | pass | All three setter-contract cases pass; every float setter holds its previous value under NaN/+Inf/-Inf and clamps at +/-1e9, with one documented exception (setSubToneLevelOffsetDb enforces no numeric range, only non-finite rejection). |
| SC-029 | pass | Per-injection: reference bit-identical up to injection, recovery counter 0->1 exactly, every OTHER slot's state bit-unchanged, poisoned slot's level strictly below reference's, resumption within a stated chunk bound. |
| SC-030 | pass | Advance-life-only parity case passes, 5.027s. |
| SC-031 | pass | Held sub-fundamental case passes, 0.190s. |

## Compliance Table — Constitutional / Cross-Cutting Constraints (CC-xxx)

| ID | Verdict | Evidence |
|---|---|---|
| CC-rt | pass | Adversarial grep for RT hazards across all 3 new headers returns only deleted copy/move ctors and prose. All audio-thread scratch is fixed-size std::array. The O(buffer) clearing path is correctly segregated from the RT-safe path (silenceAudio() O(1) vs reset() O(buffer)); deferred non-finite recovery is rate-limited. VoragoMacroMatrix::apply() is noexcept, stack-only. |
| CC-layers | pass | node tools/lint-layers.js: OK, no violations. No effects/ include anywhere in production headers (only the test's own effects-layer TU correctly includes cavern_verb.h). ODR sweep for the three new class names returns zero hits outside their defining headers. |
| CC-naming | pass | Types/enums PascalCase (12/12 verified); constants k-prefixed with zero violations; members trailing-underscore; public aggregate-config fields match the codebase convention. No plugin_ids.h touched (pure library work). |
| CC-warnings | pass | MSVC /W4: both dsp_systems_tests and dsp_effects_tests built/linked with zero warnings across all eight Phase-10 TUs. GCC/WSL -Wall -Wextra -Wpedantic: exactly one warning, pre-existing and unrelated (resonator_bank.h:493). Full-suite per-push status all green. The [long]/[.perf] timing lane is explicitly NOT fully green even in this ledger's own honesty note (three reds on a cooled re-run, none Phase-10 code) — consistent with the broader thermal-contamination pattern flagged throughout T001/T028/T030. |
| CC-portability | pass | node tools/check-portability.js: all clear, 17 TUs compiled with g++. No std::isnan/isinf/isfinite in production code; no narrowing brace-init; zero new SIMD intrinsics; only one runtime assert (compiled out under NDEBUG), rest are static_asserts. |

---

## Implementation Notes — Deviations Reported by Task Agents

This section summarizes the load-bearing deviations, decisions, and residual risks each task agent (T001-T031) reported. Full per-task detail is in the harness task notes; only the substantive ones are repeated here.

- **T001 (pre-change Seraphis baseline)** — Worst pre-change material = **Chamber** (56231 ns/block). Flagged that the clean-tree timing lane was RED on 10 unrelated cases before any Phase-10 code existed — the documented thermal/boost-clock signature, later confirmed by T030's clean isolated re-run of the same suite showing all ten green.
- **T002 (spec amendments)** — Applied all 13 amendments + Q-C/Q-D to spec.md only. Corrected a plan-side invented API name (setSmearDecoherence -> shipped setDecoherence). Made six small coherence edits beyond the literal amendment text (each tagged) so the spec would not contradict itself internally.
- **T003 (material-table generator)** — One deliberate 4th-decimal discrepancy vs. the plan's hand-computed bar-mode figures: the plan's figures require a truncated pi (3.14159); the script uses exact Math.PI and documents why it refused to degrade the constant to match a hand-computed doc value.
- **T004 (check-seraphis-green.js)** — Baseline is `git diff HEAD`, not bare `git diff`, because the test-first arm stages a deletion that bare diff cannot see. Clause logic is conditional on whether the widening has landed, so the gate also passes on a clean/untouched tree.
- **T005/T016 (shared test fixtures)** — spectralCentroidHz reused from an existing Phase-9 helper to avoid an ODR redefinition hazard. kFastAttackEnvelopeConfig writes all six stage times including two the shipped envelope never reaches, with a static_assert pinning its levels to the real shipped defaults.
- **T006 (scaffolding)** — All eight new TUs registered; the three headers wired into lint_all_headers.cpp and dsp/CMakeLists.txt with pure insertions confirming the no-compile-definition rule (B-4/Q-C) was honored.
- **T009 (FR-038 site propagation)** — **Unresolved product decision surfaced**: four kNumMaterials-sized sites in plugins/seraphis/ (dropdown_mappings.h + 3 test files) were broken by the 5->11 enum widening and are OUTSIDE every task's file list — none was fixed. Real compile-error/UB risk for Seraphis's Body Material dropdown (param ID 800) needing an explicit ruling.
- **T010-T013 (VoragoVoice core)** — Two shipped Layer-2 component ceilings clamp FR-014/FR-090's authored numbers below what they claim: MultiStageEnvelope::kMaxStageTimeMs=10000ms clamps the 20s/45s/60s stage/release times to 10s, and GrowthEnvelope::kMaxDuration=60s clamps the 120s Growth duration to 60s. The voice's own getters read back the AUTHORED numbers; what runs is clamped. Not repaired (out of task scope). T013 resolved an internal FR-020 conflict (destination-table text says "max combine" for Partial family; the Implement section's literal code is a sum) by implementing the sum, since the scheduler has no second Partial-family contribution for max to combine against.
- **T012 (mono-fold bound) — BLOCKED, then resolved.** The originally-specified 3.0dB mono-fold bound for FR-015/B-3 was measured unsatisfiable (3.6557dB measured; mathematically, more decorrelation forces a deeper mono fold, not a flatter one). Resolved by a subsequent "Q-E" ruling raising the bound to 4.0dB; no code was changed to force a pass.
- **T014/T015 (VoragoEngine)** — FR-044's steal-victim selection implemented as a public static pure function rather than requiring a live render, because processStereoBlock did not exist yet at that build point — the same pattern already used for VoragoVoice::combineWake.
- **T018-T020 (long-run/non-finite tests)** — FR-086 acceleration factor A=10 (voice-side) / A=100 (engine-side, SC-013b only) applied only to lifecycle clocks with a public setter; EcosystemEngine's step interval and TidalModulator's normalized rate cannot take the full factor (documented as conservative). A pre-existing FR-067 pre-roll defect was found and worked around in test code only (not fixed in production): setEventRateScale doesn't retroactively rescale the pre-roll drawn at reset().
- **T021/T028/T030/T031 (perf lanes) — the central open risk.** Multiple isolated [.perf]/[long] runs on a cooled, idle, P-core-pinned machine were required to get green readings; the SAME cases measured red on a warm/loaded machine or after several hours of sustained rendering (a documented, measured +14% thermal drift). T028's full isolated [long] run (prior to the 2026-09-21 re-verifications reflected in the table above) found five real failures not currently shown as failing in the ledger: VoragoEngine_OvernightSoak (SC-004b, monotonicity), VoragoEngine_OvernightEvolution (SC-005, CV), VoragoMacro_SweepAxes (SC-008, Darkness/Age/Entropy/Life plus an inverted Depth), ContinuousBody_DarkMaterialsSpectral (SC-015, mutual separation), and (via T030's separate run) VoragoVoice_CompositionOverhead/VoragoVoice_ClearingPathCost. This report does not resolve whether those were fixed before the 2026-09-21 measurements or whether the 2026-09-21 measurements are themselves stale/thermally lucky — re-running the isolated lanes (multi-hour) is required to settle it and was out of scope for this report-assembly pass.
- **T031 (Q-A kMaxVoices/polyphony ruling)** — Originally BLOCKED pending a user decision between four options for reconciling the CPU budget. The ledger's current FR-041/FR-081/SC-001 evidence shows option (A) was chosen: kMaxVoices lowered to 6, shipped polyphony fixed at 4, with the SC-001b x1.5 regression-headroom clause accepted as met only under the cooled/isolated protocol.
- **General:** Every task agent explicitly declined to relax a numeric threshold to force a pass; every red found is reported with the measured number and the responsible lever (never the bound). No task committed anything.

## Remaining Gates for the Human Loop

1. **clang-tidy** — `./tools/run-clang-tidy.ps1 -Target dsp -BuildDir build/windows-ninja` (or `all` if other plugins were touched). T030 measured 0 errors / 37 warnings, all inside Phase 10 test TUs (readability-ambiguous-smartptr-reset-call x13, bugprone-suspicious-memory-comparison x12, performance-enum-size x3, modernize-use-integer-sign-comparison x3, readability-avoid-nested-conditional-operator x2, modernize-concat-nested-namespaces x2, cppcoreguidelines-pro-type-const-cast x2). None are in production headers. Not yet fixed.
2. **Reconcile the T028/T030/T031 vs. 2026-09-21 discrepancy** (gap-list item 3) by re-running the isolated [long]/[.perf] lanes once, alone, on an idled machine, before treating this phase as genuinely green.
3. **Rule on T009's Seraphis dropdown gap** (gap-list item 7): widen param ID 800's Body Material dropdown to 11 entries, or re-point its label/clamp logic at kNumSeraphisMaterials=5. Currently unresolved and outside every task's file list.
4. **Commit** — nothing in this phase has been committed; per project policy this happens only on explicit user request.
5. **pluginval** — not applicable to this phase. Phase 10 (vorago-phase10-voice-engine) is pure dsp/ library work; no plugin (plugins/*) source was modified, so pluginval is deferred to whichever future phase (11+) wires VORAGO into an actual VST3 plugin shell.
